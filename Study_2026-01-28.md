# Chapter 1. The Theoretical Origins (이론적 기원)

고전적 금융 이론에서 마코위츠(Markowitz)의 평균-분산 최적화 이론, 즉 현대 포트폴리오 이론(MPT: Modern Portfolio Theory)은 1952년 그의 논문 「Portfolio Selection」을 통해 처음 제안되었습니다. 당시 금융시장은 자산 배분을 단순히 직관에 의존하거나 개별 자산의 기대수익률만을 중시하는 경향이 강했습니다. 그러나 이러한 접근법은 위험의 개념과 자산 간 상호관계에 대한 고려가 미흡했으며, 궁극적으로는 투자자의 잘못된 의사결정을 초래할 수 있었습니다.

마코위츠가 제시한 혁신은 기대수익뿐 아니라 분산(위험)의 개념을 함께 고려하여 포트폴리오를 수학적으로 최적화하는 틀을 마련한 점입니다. 포트폴리오 내 자산들의 수익률은 서로 상관관계를 가지므로, 분산을 단순 합산하는 것이 아니라 공분산을 반영하여 위험을 평가해야 한다는 점에 착안하였습니다. 이를 통해 여러 자산을 조합함으로써 개별 자산보다 낮은 위험을 달성할 수 있는 '분산투자(diversification)'의 원리가 체계적으로 정립되었습니다.

특히, 마코위츠는 투자자의 효용 함수가 수익률 기대치와 분산(위험)으로 표현될 수 있다고 가정하고, 이를 바탕으로 주어진 위험수준에서 기대수익을 최대화하거나 기대수익 수준에서 위험을 최소화하는 최적 포트폴리오를 수학적으로 도출하였습니다. 그 결과로 효율적 투자선(Efficient Frontier)을 발견하였으며, 이는 수학적 문제 해결과 금융 경제학적 직관이 융합된 성과라 할 수 있습니다.

이 과정에서 공분산 행렬의 역할이 핵심적이며, 투자 위험을 단일 수치가 아닌 다변량 통계적 성질로 접근하는 발상이 금융공학에 큰 전환점을 가져왔습니다. 나아가 이후 샤프(Sharpe) 등의 자본자산가격결정모델(CAPM) 발전에도 근간이 되었습니다. 즉, 마코위츠가 해결하고자 했던 문제는 단순 기대수익 극대화의 함정에서 벗어나 '위험과 수익의 균형'이라는 현실적인 투자 목표를 정량적으로 제시하는 데 있었습니다.

# Chapter 2. Mathematical Rigor (수학적 증명)

마코위츠 평균-분산 최적화 문제는 다수 자산의 포트폴리오 수익률을 벡터와 행렬로 표현하고, 라그랑주 승수법(Lagrange multiplier method)을 이용하여 효율적 투자선을 유도합니다.

우선, $N$개의 자산이 있다고 하겠습니다. 각 자산의 기대수익률을 벡터로 나타내면

$$
\boldsymbol{\mu} = \begin{bmatrix}
\mu_1 \\
\mu_2 \\
\vdots \\
\mu_N
\end{bmatrix}
$$

이고, 자산들의 수익률 공분산 행렬은

$$
\Sigma = \begin{bmatrix}
\sigma_{11} & \sigma_{12} & \cdots & \sigma_{1N} \\
\sigma_{21} & \sigma_{22} & \cdots & \sigma_{2N} \\
\vdots & \vdots & \ddots & \vdots \\
\sigma_{N1} & \sigma_{N2} & \cdots & \sigma_{NN}
\end{bmatrix}
$$

입니다. 포트폴리오의 자산 비중을 벡터 $\boldsymbol{w} = (w_1, w_2, ..., w_N)^T$로 주면, 포트폴리오 기대수익률은

$$
R_p = \boldsymbol{w}^T \boldsymbol{\mu}
$$

이고, 포트폴리오 분산(위험)은

$$
\sigma_p^2 = \boldsymbol{w}^T \Sigma \boldsymbol{w}
$$

입니다. 투자자는 일정 기대수익률 $R_p = r$을 달성하면서 위험을 최소화하려 하므로 다음 최적화 문제를 설정합니다.

$$
\min_{\boldsymbol{w}} \quad \boldsymbol{w}^T \Sigma \boldsymbol{w}
$$

subject to

$$
\boldsymbol{w}^T \boldsymbol{\mu} = r
$$

$$
\boldsymbol{w}^T \boldsymbol{1} = 1
$$

여기서 $\boldsymbol{1}$은 $N$차원 벡터로 모든 원소가 1인 벡터이며, 두 번째 제약조건은 자산 비중의 총합이 1임을 의미합니다(전 자산 투자). 단기 차입이나 공매도를 제한할 경우 $w_i \geq 0$ 제약조건이 추가될 수 있습니다.

문제를 라그랑주 승수법으로 변환하면

$$
\mathcal{L} = \boldsymbol{w}^T \Sigma \boldsymbol{w} - \lambda (\boldsymbol{w}^T \boldsymbol{\mu} - r) - \gamma (\boldsymbol{w}^T \boldsymbol{1} - 1)
$$

입니다. 편미분하여 최적 조건을 구하면

$$
\frac{\partial \mathcal{L}}{\partial \boldsymbol{w}} = 2 \Sigma \boldsymbol{w} - \lambda \boldsymbol{\mu} - \gamma \boldsymbol{1} = \boldsymbol{0}
$$

이를 정리하면

$$
\Sigma \boldsymbol{w} = \frac{\lambda}{2} \boldsymbol{\mu} + \frac{\gamma}{2} \boldsymbol{1}
$$

양변에 $\Sigma^{-1}$을 곱하고, 제약조건에 대입하여 승수 $\lambda, \gamma$를 해석적 혹은 행렬 연산으로 구합니다. 여기서 정의되는 중요한 스칼라 값들이 있습니다.

$$
A = \boldsymbol{1}^T \Sigma^{-1} \boldsymbol{1}, \quad B = \boldsymbol{1}^T \Sigma^{-1} \boldsymbol{\mu}, \quad C = \boldsymbol{\mu}^T \Sigma^{-1} \boldsymbol{\mu}
$$

효율적 투자선의 각 포트폴리오 분산은 목표 기대수익률 $r$에 대해 다음과 같이 표현됩니다.

$$
\sigma_p^2 = \frac{A r^2 - 2 B r + C}{A C - B^2}
$$

이는 매개변수 $r$을 바꿔가며 포트폴리오 위험-수익 공간에서 곡선 형태의 집합을 보여주는데, 이 곡선을 **효율적 투자선(Efficient Frontier)**이라 합니다.

특히, $r$ 값이 변할 때마다 최적 포트폴리오가 달라지고, 이 곡선 위의 포트폴리오들은 동일 위험 수준에서 최대 기대수익, 혹은 기대수익 수준에서 최소 위험을 보장합니다. 나아가, 이때의 포트폴리오 비중은 다음과 같이 형성됩니다.

$$
\boldsymbol{w} = \Sigma^{-1} \left( \alpha \boldsymbol{1} + \beta \boldsymbol{\mu} \right)
$$

여기서 $\alpha, \beta$는 라그랑주 승수를 전개한 상수로 특정 $r$값에 대응합니다.

이러한 수학적 정리 과정을 통해 마코위츠 투자 이론은 다변량 최적화와 행렬 대수학의 힘으로 금융시장에서 '위험-수익 간의 효율적 경계'를 명쾌히 제시했습니다.

# Chapter 3. Python Implementation (구현)

다음은 Python을 사용하여 실제 시장 데이터 기반으로 효율적 투자선을 그리는 구현 예제입니다. 주요 라이브러리로는 `numpy`(수치 계산), `pandas`(데이터 처리), `matplotlib`(시각화), `scipy.optimize`(최적화)를 사용합니다. 

실제 주식 데이터는 대표적으로 Yahoo Finance 환경에서 다운로드할 수 있으나, 여기서는 가상의 수익률 데이터를 생성하는 것으로 대체합니다.

```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from scipy.optimize import minimize
from typing import Tuple

def generate_synthetic_returns(n_assets: int, n_obs: int, seed: int = 42) -> pd.DataFrame:
    """
    가상의 수익률 데이터 생성 함수
    
    :param n_assets: 자산 수
    :param n_obs: 관측치(일별 수익률) 수
    :param seed: 랜덤 시드 고정
    :return: n_obs x n_assets 크기의 수익률 DataFrame
    """
    np.random.seed(seed)
    mean_returns = np.random.uniform(0.0005, 0.002, n_assets)  # 0.05%~0.2% 일평균 수익률
    cov_matrix = np.random.rand(n_assets, n_assets)
    cov_matrix = np.dot(cov_matrix, cov_matrix.T)  # 대칭 양의 정부호 행렬 생성
    returns = np.random.multivariate_normal(mean_returns, cov_matrix, n_obs)
    columns = [f'Asset_{i+1}' for i in range(n_assets)]
    return pd.DataFrame(returns, columns=columns)

def portfolio_statistics(weights: np.ndarray, mean_returns: np.ndarray, cov_matrix: np.ndarray) -> Tuple[float, float]:
    """
    주어진 자산 비중에 대한 기대 수익률과 위험(표준편차) 계산
    
    :param weights: 자산별 투자 비중 배열
    :param mean_returns: 자산별 기대수익률 배열
    :param cov_matrix: 자산별 수익률 공분산 행렬
    :return: (포트폴리오 기대수익률, 포트폴리오 위험(표준편차))
    """
    portfolio_return = np.dot(weights, mean_returns)
    portfolio_variance = np.dot(weights.T, np.dot(cov_matrix, weights))
    portfolio_std = np.sqrt(portfolio_variance)
    return portfolio_return, portfolio_std

def min_variance_portfolio(mean_returns: np.ndarray, cov_matrix: np.ndarray, target_return: float) -> np.ndarray:
    """
    주어진 목표 기대수익률을 만족하는 최소 분산 포트폴리오 비중 계산
    
    :param mean_returns: 자산별 기대수익률 배열
    :param cov_matrix: 자산별 공분산 행렬
    :param target_return: 목표 기대수익률
    :return: 최적화된 자산별 비중 배열
    """
    n = len(mean_returns)

    # 제약조건 함수 정의
    constraints = (
        {'type': 'eq', 'fun': lambda w: np.sum(w) - 1},                            # 투자 비중 합 = 1
        {'type': 'eq', 'fun': lambda w: np.dot(w, mean_returns) - target_return}  # 목표 기대수익 = target_return
    )
    bounds = tuple((0, 1) for _ in range(n))  # 공매도 제한: 0~1 비중

    # 초기 guess (균등 분배)
    init_guess = np.array(n * [1. / n])

    # 목적함수: 포트폴리오 분산 최소화
    def portfolio_var(w):
        return np.dot(w.T, np.dot(cov_matrix, w))

    result = minimize(portfolio_var, init_guess,
                      method='SLSQP', bounds=bounds, constraints=constraints)
    if not result.success:
        raise BaseException("최적화 실패: " + result.message)
    return result.x

def efficient_frontier(mean_returns: np.ndarray, cov_matrix: np.ndarray, returns_range: np.ndarray) -> Tuple[np.ndarray, np.ndarray]:
    """
    여러 목표 기대수익률에 대해 최소분산 포트폴리오 위험을 계산
    
    :param mean_returns: 자산별 기대수익률 배열
    :param cov_matrix: 공분산 행렬
    :param returns_range: 목표 기대수익률 배열
    :return: tuple(포트폴리오 위험 배열, 자산비중 행렬)
    """
    risks = []
    portfolios = []

    for r in returns_range:
        weights = min_variance_portfolio(mean_returns, cov_matrix, r)
        _, risk = portfolio_statistics(weights, mean_returns, cov_matrix)
        risks.append(risk)
        portfolios.append(weights)

    return np.array(risks), np.array(portfolios)

def main():
    # 5개 자산, 250 거래일 가상 데이터 생성
    returns_df = generate_synthetic_returns(n_assets=5, n_obs=250)
    mean_returns = returns_df.mean().values
    cov_matrix = returns_df.cov().values

    # 목표 기대수익률 범위 지정 (최소~최대 평균수익률 구간)
    target_returns = np.linspace(mean_returns.min(), mean_returns.max(), 50)

    # 효율적 투자선 계산
    risks, portfolios = efficient_frontier(mean_returns, cov_matrix, target_returns)

    # 시각화
    plt.figure(figsize=(10, 6))
    plt.plot(risks, target_returns, 'b--', label='Efficient Frontier (Min Variance)')
    plt.xlabel('Risk (Standard Deviation)')
    plt.ylabel('Expected Return')
    plt.title('Efficient Frontier of Portfolio')
    plt.legend()
    plt.grid(True)
    plt.show()

if __name__ == "__main__":
    main()
```

---

### 코드 설명

- `generate_synthetic_returns` 함수는 임의의 기대수익률과 공분산 행렬을 갖는 다중 정규 분포 기반 수익률 데이터를 생성합니다.
- `portfolio_statistics`는 주어진 포트폴리오 가중치에 따른 기대수익과 위험을 계산합니다.
- `min_variance_portfolio`는 목표 기대수익률을 만족하면서 포트폴리오 위험을 최소화하는 최적화 문제를 `scipy.optimize.minimize`로 해결합니다.
- `efficient_frontier`는 다양한 목표 기대수익률 값에 대해 최소 분산 포트폴리오를 연속적으로 구하여 효율적 투자선을 만듭니다.
- 마지막으로 `main` 함수는 위 과정을 실행하고 결과를 리스크-수익 그래프로 시각화합니다.

이 코드는 대학원 수준의 퀀트 지망생이 최소한의 수학적 이해와 Python 구현 능력을 키우는 데 적합합니다.

# Chapter 4. Real World & Critical View (한계와 비평)

마코위츠 MPT는 금융공학의 혁명적 성과이나 현실 시장에서 아래와 같은 한계와 비판에 직면해 왔습니다.

1. **정규분포 가정과 리스크 측정의 한계**  
   MPT는 자산 수익률이 정상분포를 따른다고 가정하고 위험을 분산(Variance)으로 표현합니다. 그러나 실제 시장 수익률은 종종 꼬리가 두껍고 왜도가 존재하며, 극단적 리스크(Tail Risk)를 반영하지 못합니다. 이로 인한 '블랙 스완' 사건 발생 시 모델은 위험을 과소평가하게 됩니다.

2. **공분산 행렬의 불안정성 및 추정 오류 문제**  
   공분산 행렬은 역사적 데이터에 기반해 추정되며, 샘플 부족이나 시장 변동성 증가 시 왜곡될 수 있습니다. 이는 포트폴리오 최적화의 해에 큰 영향을 주어, 실제 적용 시 과도한 변화나 불안정성을 초래하기 쉽습니다. 이런 문제를 해소하려는 리스크 팩터 모델과 축소 기법이 연구되어 왔습니다.

3. **비현실적인 제약조건 부재**  
   원래 MPT는 단기 차입, 공매도 무제한 등 현실과 괴리된 가정을 합니다. 실제 투자환경에서는 레버리지 한도, 거래 비용, 세금, 시장 유동성 등이 필연적 조건인데 이를 반영하지 않으면 최적 포트폴리오가 실행 불가능할 수 있습니다.

4. **시장 효율성 가정과 투자자 합리성 문제**  
   MPT는 투자자가 합리적이며 위험회피적이라는 경제학적 가정을 전제로 합니다. 행동금융학적 연구결과 투자자들이 항상 합리적이지 않으며, 딜레마가 생길 수 있음을 지적합니다.

5. **비판적 사례: LTCM과 2008 금융위기**  
   1998년 LTCM(Long-Term Capital Management)은 MPT 및 블랙-숄즈 모델을 기반으로 과도한 레버리지 투자 후 러시아 디폴트 사건 등으로 엄청난 손실을 냈습니다. 2008년 글로벌 금융위기 또한 신용파생상품 리스크가 과소평가된 결과로, 위험 측정 모델 한계가 드러났습니다.

---

## 실무 팁 (Practice Tip)

- **리스크 관리에 다변량 분산 외 다른 지표 추가**  
  예: Value-at-Risk, Conditional VaR 등 꼬리 위험을 보완하여 포트폴리오를 평가합니다.
  
- **공분산 행렬 추정 방법 개선**  
  과거 단순 샘플 공분산 대신 이동평균, EWMA(Exponentially Weighted Moving Average), 팩터 모델로 노이즈를 줄이는 노력이 필요합니다.

- **제약 조건 강화 및 시뮬레이션 추가**  
  레버리지 제한, 거래 비용, 최소 투자 단위 등 현실적 제약을 최적화 모델에 반영함과 동시에 몬테카를로 시뮬레이션으로 전체 위험성을 점검합니다.

- **동적 자산 배분 전략 활용**  
  시장 환경 변화에 따라 위험 조정 비중을 유연하게 바꾸는 동적 포트폴리오 이론 적용을 권장합니다.

- **실시간 데이터 및 머신러닝 활용**  
  공분산 및 수익률 예측 정확도를 높이기 위해 머신러닝 기반 모델을 보조적으로 활용하는 추세입니다.

---

이상으로, 효율적 투자선의 유도부터 실제 구현, 그리고 현실 적용 시 고려해야 할 한계와 보완점에 대해 심층적으로 살펴보았습니다. 금융공학 석좌교수로서 여러분이 이론적 근거와 실무적 한계를 모두 체득하여, 진정한 의미의 '책임 있는 금융공학자'로 성장하기를 기대합니다.