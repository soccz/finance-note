# Chapter 1. The Theoretical Origins (이론적 기원)

마코위츠가 현대 포트폴리오 이론(Modern Portfolio Theory, MPT)을 발표한 1950년대는 전후 복구와 경제성장으로 자본시장에 대한 체계적 접근이 필요했던 시기였습니다. 이전까지 투자자는 대부분 개별 자산의 기대수익률에만 관심을 가졌으며 리스크는 직관적 수준에서만 고려되었습니다. 하지만 1차대전과 대공황, 그리고 2차대전 이후의 금융시장은 고도의 변동성과 불확실성을 나타냈고, 투자자들이 리스크 분산의 중요성을 인지하게 된 계기가 되었습니다.

해리 마코위츠는 투자자들이 개별 자산의 위험과 수익률만을 단순 비교하는 것이 아니라, 이들 자산 간의 상호 상관관계(공분산)를 분석하여 포트폴리오 전반의 위험을 최소화하는 문제로 전환할 것을 제안했습니다. 특히 중요한 점은 “분산투자(diversification)” 개념을 수학적으로 엄밀히 정립하고 효율적 투자선(Efficient Frontier)을 유도했으며, 이는 현대 금융공학의 초석이 되었습니다.

마코위츠의 핵심 의문은 단순히 개별증권 수익률의 통계치가 아니라, 어떻게 자산들을 조합하면 동일한 기대수익 대비 변동성(위험)을 최소화할 수 있는가 하는 것이었습니다. 이를 위해 자산 간 통계적 관계를 고려한 포트폴리오 리스크 측정체계가 필요했고, 포트폴리오의 공분산 행렬은 이 문제의 열쇠가 되었습니다. 즉, 개별 자산 위험뿐 아니라 상호관계까지 파악한 투자 전략이야말로 불확실한 시장에서의 합리적 의사결정의 초석입니다.

# Chapter 2. Mathematical Rigor (수학적 증명)

효율적 투자선의 수학적 유도는 다음 문제에서 출발합니다. 주어진 $N$개의 자산의 기대수익률 벡터와 공분산 행렬이 주어질 때, 특정 기대수익률 $\mu_p$를 보장하면서 포트폴리오의 분산을 최소화하는 자산 배분 비율($\mathbf{w}$)을 찾는 문제입니다.

각 자산의 기대수익률을 벡터
$$
\boldsymbol{\mu} = 
\begin{bmatrix}
\mu_1 \\
\mu_2 \\
\vdots \\
\mu_N
\end{bmatrix}
$$
라 하고, 공분산 행렬을
$$
\Sigma = 
\begin{bmatrix}
\sigma_{11} & \sigma_{12} & \cdots & \sigma_{1N} \\
\sigma_{21} & \sigma_{22} & \cdots & \sigma_{2N} \\
\vdots & \vdots & \ddots & \vdots \\
\sigma_{N1} & \sigma_{N2} & \cdots & \sigma_{NN}
\end{bmatrix}
$$
라 정의하면, 포트폴리오 수익률과 분산은 각각
$$
\mu_p = \mathbf{w}^\top \boldsymbol{\mu}, \quad \sigma_p^2 = \mathbf{w}^\top \Sigma \mathbf{w}
$$
로 표현됩니다.

포트폴리오 가중치 벡터 $\mathbf{w}$는 자산별 투자 비율로, 정상적으로 모든 투자액이 합쳐져야 하므로
$$
\sum_{i=1}^N w_i = \mathbf{w}^\top \mathbf{1} = 1
$$
이어야 하고, 투자 목표 기대수익률은
$$
\mathbf{w}^\top \boldsymbol{\mu} = \mu_p
$$
를 만족해야 합니다.

따라서 최적화 문제는 다음과 같은 제약조건을 가진 2차 형식의 함수 최소화 문제입니다:

$$
\begin{aligned}
&\min_{\mathbf{w}} \quad \mathbf{w}^\top \Sigma \mathbf{w} \\
&\text{subject to} \quad
\begin{cases}
\mathbf{w}^\top \boldsymbol{\mu} = \mu_p \\
\mathbf{w}^\top \mathbf{1} = 1
\end{cases}
\end{aligned}
$$

이 문제는 라그랑주 승수법(Lagrange Multiplier)를 통해 다음 라그랑지안(Lagrangian)을 정의하고 풀 수 있습니다.

$$
\mathcal{L}(\mathbf{w}, \lambda, \gamma) = \mathbf{w}^\top \Sigma \mathbf{w} - \lambda (\mathbf{w}^\top \boldsymbol{\mu} - \mu_p) - \gamma (\mathbf{w}^\top \mathbf{1} - 1)
$$

여기서 $\lambda, \gamma$는 각각 기대수익률과 무조건 합 벡터 조건에 대응하는 라그랑주 승수입니다. 최적화 조건은 편미분이 0이 되는 것입니다:

$$
\frac{\partial \mathcal{L}}{\partial \mathbf{w}} = 2 \Sigma \mathbf{w} - \lambda \boldsymbol{\mu} - \gamma \mathbf{1} = \mathbf{0} \quad \Rightarrow \quad 2 \Sigma \mathbf{w} = \lambda \boldsymbol{\mu} + \gamma \mathbf{1}
$$

이를 정리하면

$$
\mathbf{w} = \frac{1}{2} \Sigma^{-1} (\lambda \boldsymbol{\mu} + \gamma \mathbf{1})
$$

이제 두 조건 $\mathbf{w}^\top \boldsymbol{\mu} = \mu_p$ 와 $\mathbf{w}^\top \mathbf{1} = 1$ 를 적용하여 $\lambda$와 $\gamma$를 결정합니다. 식을 행렬 형태로 쓰면

$$
\begin{cases}
\boldsymbol{\mu}^\top \mathbf{w} = \mu_p  \\
\mathbf{1}^\top \mathbf{w} = 1 
\end{cases}
\Rightarrow
\begin{cases}
\boldsymbol{\mu}^\top \Sigma^{-1} (\lambda \boldsymbol{\mu} + \gamma \mathbf{1}) = 2 \mu_p \\
\mathbf{1}^\top \Sigma^{-1} (\lambda \boldsymbol{\mu} + \gamma \mathbf{1}) = 2 
\end{cases}
$$

편의를 위해 다음 행렬 및 벡터들을 정의합니다.

$$
A = \mathbf{1}^\top \Sigma^{-1} \mathbf{1}, \quad B = \mathbf{1}^\top \Sigma^{-1} \boldsymbol{\mu}, \quad C = \boldsymbol{\mu}^\top \Sigma^{-1} \boldsymbol{\mu}
$$

그러면 연립방정식은

$$
\begin{bmatrix}
C & B \\
B & A
\end{bmatrix}
\begin{bmatrix}
\lambda \\
\gamma
\end{bmatrix}
= 2
\begin{bmatrix}
\mu_p \\
1
\end{bmatrix}
$$

이다. 여기서 $(\lambda, \gamma)$를 풀고, 다시 가중치 벡터

$$
\mathbf{w} = \frac{1}{2} \Sigma^{-1} (\lambda \boldsymbol{\mu} + \gamma \mathbf{1})
$$

을 구하면, 주어진 기대수익률 $\mu_p$를 가지면서 변동성이 최소화되는 최적 포트폴리오를 확보할 수 있습니다.

이러한 일련의 과정들을 통해 다양한 $\mu_p$에 대해 최적화된 분산 최소 포트폴리오 조합 $\mathbf{w}^*$를 찾아, $ (\sigma_p, \mu_p)$의 좌표를 플롯하면 바로 효율적 투자선(Efficient Frontier)이 도출됩니다.

# Chapter 3. Python Implementation (구현)

아래 코드는 위에서 이론적으로 유도한 효율적 투자선을 Python으로 구현한 예제입니다. `numpy`로 행렬 연산을 수행하고 `scipy.optimize`의 `minimize`를 활용하는 방식을 사용합니다. 주석을 통해 각 단계의 의미와 수학적 배경도 설명합니다.

```python
import numpy as np
import pandas as pd
from scipy.optimize import minimize
import matplotlib.pyplot as plt
from typing import Tuple

def portfolio_variance(weights: np.ndarray, cov_matrix: np.ndarray) -> float:
    """
    포트폴리오 분산 계산 함수
    
    Args:
        weights (np.ndarray): 자산 비중 벡터 (N,)
        cov_matrix (np.ndarray): 자산 공분산 행렬 (N, N)
    
    Returns:
        float: 포트폴리오 분산
    """
    return weights.T @ cov_matrix @ weights

def efficient_frontier(
    returns: pd.DataFrame, 
    points: int = 50
) -> Tuple[np.ndarray, np.ndarray]:
    """
    기대수익률 범위에 따른 효율적 투자선을 산출
    
    Args:
        returns (pd.DataFrame): 각 자산별 수익률 데이터프레임 (기간 x 자산수)
        points (int): 효율적 투자선 상에서 샘플링할 포인트 개수
    
    Returns:
        Tuple[np.ndarray, np.ndarray]: 포트폴리오 변동성, 기대수익률 배열
    """
    mean_returns = returns.mean().values  # 자산별 기대수익률
    cov_matrix = returns.cov().values     # 자산별 공분산 행렬
    n = len(mean_returns)                 # 자산 개수
    
    # 최소 분산 포트폴리오부터 최대 기대수익 포트폴리오까지 범위 설정
    min_return = mean_returns.min()
    max_return = mean_returns.max()
    target_returns = np.linspace(min_return, max_return, points)
    
    portfolio_vars = []
    portfolio_means = []

    # 목적함수: 포트폴리오 분산 최소화
    def objective(weights):
        return portfolio_variance(weights, cov_matrix)

    # 구분할 조건들: 투자의 합은 1이며, 목표 기대수익률 달성
    constraints = (
        {'type': 'eq', 'fun': lambda w: np.sum(w) - 1},
        {'type': 'eq', 'fun': None}  # 목표 기대수익률 부분은 반복문 내 재정의
    )
    
    bounds = tuple((0.0, 1.0) for _ in range(n))  # 숏 셀링 금지 가정

    for mu_p in target_returns:
        constraints = (
            {'type': 'eq', 'fun': lambda w: np.sum(w) - 1},
            {'type': 'eq', 'fun': lambda w, target=mu_p: w @ mean_returns - target}
        )
        init_guess = np.repeat(1/n, n)
        result = minimize(
            objective,
            init_guess,
            method='SLSQP',
            bounds=bounds,
            constraints=constraints
        )
        if result.success:
            portfolio_vars.append(objective(result.x))
            portfolio_means.append(mu_p)
        else:
            # 최적화 실패 시 NaN으로 표시
            portfolio_vars.append(np.nan)
            portfolio_means.append(mu_p)
    
    return np.sqrt(portfolio_vars), portfolio_means

def plot_efficient_frontier(
    volatilities: np.ndarray,
    returns: np.ndarray
) -> None:
    """
    효율적 투자선을 시각화하는 함수
    
    Args:
        volatilities (np.ndarray): 포트폴리오 변동성 배열
        returns (np.ndarray): 포트폴리오 기대수익률 배열
    """
    plt.figure(figsize=(10, 6))
    plt.plot(volatilities, returns, 'b--', label='Efficient Frontier')
    plt.xlabel('Portfolio Volatility (Standard Deviation)')
    plt.ylabel('Expected Return')
    plt.title('Efficient Frontier')
    plt.grid(True)
    plt.legend()
    plt.show()

if __name__ == '__main__':
    # 예시: 금융 시계열 데이터 로드 (임의의 수익률 DataFrame 생성)
    np.random.seed(42)
    dates = pd.date_range(start='2020-01-01', periods=500)
    # 4종목 무작위 수익률 생성, 상관관계 및 변동성 포함
    base_returns = np.random.multivariate_normal(
        mean=[0.0005, 0.0007, 0.0004, 0.0006], 
        cov=[[0.0004, 0.0001, 0.00005, 0.00002],
             [0.0001, 0.0003, 0.00001, 0.00006],
             [0.00005, 0.00001, 0.0002, 0.00003],
             [0.00002, 0.00006, 0.00003, 0.00025]],
        size=500
    )
    returns_df = pd.DataFrame(base_returns, index=dates, columns=['Asset1', 'Asset2', 'Asset3', 'Asset4'])

    # 효율적 투자선 산출
    volatilities, expected_returns = efficient_frontier(returns_df, points=100)

    # 결과 시각화
    plot_efficient_frontier(volatilities, expected_returns)
```

---

### 코드 설명 요약

- `portfolio_variance`: 포트폴리오 가중치와 공분산 행렬을 입력받아 포트폴리오 분산을 계산합니다.
- `efficient_frontier`: 주어진 자산 수익률 데이터를 통해 0~100포인트 범위 내 목표 기대수익률별 최소 분산 포트폴리오를 산출합니다.
- `minimize` 함수의 `SLSQP` 메서드는 제약조건과 경계조건이 있는 최적화에 적합합니다.
- 실제 투자에서는 숏포지션을 허용하지 않는다는 가정하에 각 자산 가중치는 0 이상 1 이하로 제한했습니다.
- 산출 포트폴리오들의 변동성과 기대수익률을 선형 플롯하여 효율적 투자선을 나타냅니다.

# Chapter 4. Real World & Critical View (한계와 비평)

MPT는 투자최적화에 혁신을 가져왔지만, 현실 세계 금융시장은 여러 이유로 MPT의 가정을 벗어나는 현상을 보입니다.

1. **변동성만으로 리스크를 전부 설명할 수 없다**  
   MPT는 위험을 표준편차(변동성)로 환원하는 데, 이는 수익률 분포가 정규분포를 따른다는 가정을 기반으로 합니다. 그러나 금융자산의 수익률은 종종 비대칭적이고 꼬리가 두꺼운 분포(heavy tails)를 보이며, 극단적 손실(Black Swan events)을 고려하지 못합니다.

2. **공분산 행렬의 불안정성 문제**  
   금융시장 데이터가 시계열적으로 비정상적이고 노이즈가 많아 공분산 행렬이 매우 불안정합니다. 결과적으로 최적화 결과가 과대적합(Overfitting)되어 실제 투자에 적용시 성능이 떨어질 수 있습니다. 금융위기 시기 공분산 구조가 급변하여 과거에 추정된 수치들이 무용해지는 현상이 있습니다.

3. **유동성 및 거래비용 무시**  
   마코위츠 모델은 완전 유동성 시장과 거래 비용 회피를 전제로 합니다. 실제 투자환경에서는 거래비용, 세금, 유동성 제약으로 인해 자주 리밸런싱 하는 것이 어렵고 비용이 큽니다.

4. **숏 포지션과 레버리지 제한 및 투자자 심리 미반영**  
   마코위츠 모델은 전통적으로 무제한 숏셀링, 레버리지를 암묵적으로 허용하지만 현실적으로 많은 투자자는 이 규제를 받습니다. 또한 실제 투자 의사결정에 영향을 주는 behavior finance 요소들은 전혀 반영하지 못합니다.

---

### 실무 팁 및 보완책

- **정규화 된 기대수익률 및 리스크 추정법(Exponentially Weighted Moving Average, EWMA)**  
  최근 관측치에 중점을 둔 가중치 부여로 보다 동적인 위험 추정 가능

- **강인한 최적화 기법 사용 (Robust Optimization)**  
  공분산 행렬의 추정 불확실성 및 오류를 감안한 보수적 최적화 방법 적용

- **Black-Litterman 모델 활용**  
  투자자의 주관적 전망을 통계적 시장 데이터와 결합하여 합리적 기대수익률 산출

- **리스크 측도 다변화 (VaR, CVaR 등 사용)**  
  변동성 외에 꼬리 위험(tail risk) 관리 도구 보완

- **포트폴리오 제약조건 현실화**  
  최소 투자 단위, 거래비용 등을 고려한 실제 가능한 투자전략 수립

- **시뮬레이션과 스트레스 테스트 병행**  
  위기 시 시나리오 분석과 몬테카를로 시뮬레이션으로 모델 한계 대비

---

### 결론

효율적 투자선 유도는 현대 금융공학에 근본적으로 기여한 개념이며, 합리적 투자의 토대가 되는 이론입니다. 그러나 금융시장의 복잡성과 예측 불가능성을 온전히 반영하지 못하는 한계가 분명 존재합니다. 따라서 이론의 근본을 이해하는 동시에, 현실을 반영하는 다양한 고도화 기법을 함께 습득하는 것이 현명합니다. 컴퓨터 공학적 접근으로 강력한 데이터 처리 및 최적화 능력을 갖춘 학습자라면, 이론과 실무를 유기적으로 결합하여 한층 발전된 퀀트 전략을 설계할 수 있을 것입니다.