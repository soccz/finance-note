# Chapter 1. The Theoretical Origins (이론적 기원)

현대 포트폴리오 이론(Modern Portfolio Theory, MPT)은 1950년대 해리 마코위츠(Harry Markowitz)에 의해 체계화되었습니다. 그 이전 금융시장은 주식 개별 종목의 기대수익률에 치중하는 경향이 강했습니다. 투자자들은 개별 자산의 수익률 분석에만 집중하였으나, 마코위츠는 자산군 간의 상호 상관관계가 포트폴리오의 리스크 관리에 본질적임을 깨달았습니다. 그의 혁신은 단순히 최댓값을 추구하는 투자가 아니라, 위험과 수익률의 균형을 맞추는 다차원 최적화 문제를 도입한 데 있습니다.

마코위츠가 공분산(Covariance)이라는 개념을 도입한 배경에는 두 가지 하자가 있었습니다. 첫째, 자산들의 위험이 삼성전자, 현대차 등 서로 독립적이지 않고 연관되어 있다는 점이며, 둘째, 변동성이 큰 자산이라도 다른 자산과의 상관관계에 따라 전반적인 포트폴리오 위험이 줄어들 수 있다는 사실입니다. 실제로 서로 반대 방향으로 움직이는 자산을 적절히 조합하면 전체 위험을 줄이면서 기대수익률을 높이는 ‘효율적인 프론티어’를 구축할 수 있습니다.

마코위츠가 고안한 이론은 마치 열역학에서 에너지 분포의 최적화를 고민하는 물리학자처럼, 불확실한 금융시장 내에서 자산의 분배를 최적화하고자 하는 시도로 볼 수 있습니다. 또한 이론적 정교함과 더불어 이 문제를 푸는 방식은 현대 수학적 최적화 이론과 컴퓨터 계산 능력의 진보에 힘입어 그 실용성과 정확성을 더해갔습니다. 현대 금융공학에서 MPT는 자산배분, 위험관리의 근간을 이루는 핵심 모델입니다.

# Chapter 2. Mathematical Rigor (수학적 증명)

마코위츠 포트폴리오 최적화 문제는 다음과 같이 정의됩니다. 투자할 자산이 $N$개 존재하고, 각 자산의 수익률은 랜덤 변수로 표현됩니다. 기대수익률 벡터는 

$$
\boldsymbol{\mu} = 
\begin{bmatrix}
\mu_1 \\
\mu_2 \\
\vdots \\
\mu_N
\end{bmatrix}
$$ 

이며, 자산 간 공분산 행렬은

$$
\Sigma = 
\begin{bmatrix}
\sigma_{11} & \sigma_{12} & \cdots & \sigma_{1N} \\
\sigma_{21} & \sigma_{22} & \cdots & \sigma_{2N} \\
\vdots & \vdots & \ddots & \vdots \\
\sigma_{N1} & \sigma_{N2} & \cdots & \sigma_{NN} \\
\end{bmatrix}
$$ 

로 나타냅니다. 각각의 $\sigma_{ij} = \mathrm{Cov}(R_i, R_j)$는 자산 $i$와 $j$의 수익률 간 공분산입니다.

투자 비중 벡터를

$$
\boldsymbol{w} = 
\begin{bmatrix}
w_1 \\
w_2 \\
\vdots \\
w_N
\end{bmatrix}
$$ 

라고 하면, 포트폴리오의 기대수익률은 

$$
R_p = \boldsymbol{w}^\top \boldsymbol{\mu} = \sum_{i=1}^N w_i \mu_i,
$$ 

포트폴리오의 분산(리스크 제곱)은

$$
\sigma_p^2 = \boldsymbol{w}^\top \Sigma \boldsymbol{w}.
$$ 

마코위츠의 최적화 문제는 주어진 기대수익률 $R_p^*$에 대해 가장 위험(분산)이 최소가 되도록 하거나, 또는 주어진 리스크 $\sigma_p^*$ 하에 기대수익률을 최대화하는 형태로 설정할 수 있습니다.

다음은 기대수익률이 최소값으로 고정된 상태에서 포트폴리오 위험을 최소화하는 문제입니다:

$$
\begin{aligned}
& \min_{\boldsymbol{w}} \quad \boldsymbol{w}^\top \Sigma \boldsymbol{w} \\
& \text{subject to} \\
& \boldsymbol{w}^\top \boldsymbol{\mu} = R_p^* \\
& \boldsymbol{w}^\top \mathbf{1} = 1
\end{aligned}
$$

여기서 $\mathbf{1}$은 모든 원소가 1인 $N \times 1$ 벡터입니다 (모든 투자 비중의 합은 1 즉, 전 자본을 투자한다는 뜻).

이 문제를 라그랑주 승수법(Lagrange Multipliers)을 통해 풉니다. 목적함수에 두 개의 제약을 곱한 라그랑주 함수는

$$
\mathcal{L}(\boldsymbol{w}, \lambda, \gamma) = \boldsymbol{w}^\top \Sigma \boldsymbol{w} - \lambda (\boldsymbol{w}^\top \boldsymbol{\mu} - R_p^*) - \gamma (\boldsymbol{w}^\top \mathbf{1} - 1).
$$

$\boldsymbol{w}$에 대해 미분하여 0으로 두면

$$
2 \Sigma \boldsymbol{w} - \lambda \boldsymbol{\mu} - \gamma \mathbf{1} = 0.
$$

해당 식을 풀어 최적 투자 비중 $\boldsymbol{w}^*$를 구할 수 있습니다. 행렬 대수와 벡터 미적분을 체계적으로 적용하여 다음과 같이 해를 표현할 수 있습니다:

$$
\boldsymbol{w}^* = \frac{1}{2} \Sigma^{-1} \left( \lambda \boldsymbol{\mu} + \gamma \mathbf{1} \right).
$$

라그랑주 승수 $\lambda$, $\gamma$는 두 제약조건으로부터 유도되며 다음 선형 시스템을 만족합니다:

$$
\begin{bmatrix}
\boldsymbol{\mu}^\top \Sigma^{-1} \boldsymbol{\mu} & \boldsymbol{\mu}^\top \Sigma^{-1} \mathbf{1} \\
\mathbf{1}^\top \Sigma^{-1} \boldsymbol{\mu} & \mathbf{1}^\top \Sigma^{-1} \mathbf{1}
\end{bmatrix}
\begin{bmatrix}
\lambda \\
\gamma
\end{bmatrix}
=
2
\begin{bmatrix}
R_p^* \\
1
\end{bmatrix}.
$$

이 선형 시스템을 푼 후, $\lambda$와 $\gamma$를 식에 대입해 최적 비중을 구할 수 있습니다. 이 과정을 통해 마코위츠는 위험과 수익률 간의 트레이드오프 최적해를 명확히 수학화했습니다.

# Chapter 3. Python Implementation (구현)

이번 절에서는 Python `scipy.optimize`의 `minimize` 함수를 사용하여 마코위츠 포트폴리오 최적화 문제를 풀어보고, 효율적 프론티어(efficient frontier)를 시각화하는 과정을 실습합니다. 대상 데이터는 4개 자산의 일별 수익률로 가정합니다.

```python
import numpy as np
import pandas as pd
from scipy.optimize import minimize
import matplotlib.pyplot as plt

def portfolio_return(weights: np.ndarray, mean_returns: np.ndarray) -> float:
    """
    포트폴리오 기대수익률 계산

    Parameters
    ----------
    weights : np.ndarray
        자산별 투자비중 (합 = 1)
    mean_returns : np.ndarray
        각 자산의 기대수익률 벡터

    Returns
    -------
    float
        포트폴리오 기대수익률
    """
    return np.dot(weights, mean_returns)

def portfolio_volatility(weights: np.ndarray, cov_matrix: np.ndarray) -> float:
    """
    포트폴리오 변동성 (리스크) 계산

    Parameters
    ----------
    weights : np.ndarray
        자산별 투자비중 (합 = 1)
    cov_matrix : np.ndarray
        자산수익률 공분산 행렬

    Returns
    -------
    float
        포트폴리오 리스크 (변동성)
    """
    return np.sqrt(np.dot(weights.T, np.dot(cov_matrix, weights)))

def optimize_portfolio(mean_returns: np.ndarray, cov_matrix: np.ndarray, target_return: float) -> dict:
    """
    목표 기대수익률을 만족하면서 변동성을 최소화하는 포트폴리오 최적화

    Parameters
    ----------
    mean_returns : np.ndarray
        각 자산 기대수익률
    cov_matrix : np.ndarray
        자산 공분산 행렬
    target_return : float
        투자자가 설정한 목표 기대수익률

    Returns
    -------
    dict
        최적화 결과 {'weights': 최적 비중, 'fun': 최소 변동성}
    """
    num_assets = len(mean_returns)

    # 초기값: 균등 배분
    init_guess = np.ones(num_assets) / num_assets

    # 제약조건: 합이 1
    constraints = (
        {'type': 'eq', 'fun': lambda weights: np.sum(weights) - 1},
        {'type': 'eq', 'fun': lambda weights: portfolio_return(weights, mean_returns) - target_return}
    )

    # 투자비중 범위 (0 ~ 1) : 단기 매도 불가 또는 공매도 금지 조건 가정
    bounds = tuple((0.0, 1.0) for _ in range(num_assets))

    # 최적화 수행 - 목적함수: 변동성 최소화
    result = minimize(
        fun=lambda w: portfolio_volatility(w, cov_matrix),
        x0=init_guess,
        method='SLSQP',
        bounds=bounds,
        constraints=constraints,
        options={'disp': False}
    )

    return result

def generate_efficient_frontier(mean_returns: np.ndarray, cov_matrix: np.ndarray, returns_range: np.ndarray) -> tuple:
    """
    다양한 목표 기대수익률에 따른 효율적 프론티어 산출

    Parameters
    ----------
    mean_returns : np.ndarray
        자산 기대수익률
    cov_matrix : np.ndarray
        공분산 행렬
    returns_range : np.ndarray
        목표 기대수익률 값의 배열

    Returns
    -------
    tuple
        (list_of_risks, list_of_weights)
    """
    risks = []
    weights_list = []

    for target_return in returns_range:
        optimized = optimize_portfolio(mean_returns, cov_matrix, target_return)
        if optimized.success:
            risks.append(optimized.fun)
            weights_list.append(optimized.x)
        else:
            risks.append(None)
            weights_list.append(None)

    return risks, weights_list

def main():
    # 데이터 생성: 4개 자산 기대수익률과 공분산 행렬 예시
    np.random.seed(42)
    mean_returns = np.array([0.1, 0.12, 0.14, 0.08])  # 연간 기대수익률 (10%, 12%, 14%, 8%)
    cov_matrix = np.array([
        [0.005, -0.010, 0.004, -0.002],
        [-0.010, 0.040, -0.002, 0.004],
        [0.004, -0.002, 0.023, 0.002],
        [-0.002, 0.004, 0.002, 0.010]
    ])  # 연간 공분산 행렬

    # 목표 기대수익률 범위 (0.08 ~ 0.14)
    target_returns = np.linspace(0.08, 0.14, 50)

    # 효율적 프론티어 산출
    risks, weights = generate_efficient_frontier(mean_returns, cov_matrix, target_returns)

    # 시각화
    plt.figure(figsize=(10, 6))
    plt.plot(risks, target_returns, 'b--', label='Efficient Frontier')
    plt.xlabel('Risk (Volatility)')
    plt.ylabel('Expected Return')
    plt.title('Efficient Frontier - Markowitz Portfolio Optimization')
    plt.legend()
    plt.grid(True)
    plt.show()

if __name__ == '__main__':
    main()
```

### 코드 설명 요약

- `portfolio_return` 함수는 자산별 투자 비중(weights)과 기대수익률 벡터의 내적을 통해 총 기대수익률을 계산합니다.
- `portfolio_volatility`는 투자 비중과 공분산 행렬간 이차형식(quadratic form) 계산으로 포트폴리오 변동성(리스크)을 구합니다.
- `optimize_portfolio` 함수는 목표 기대수익률을 제약조건으로 잡고, 변동성을 최소화하는 투자 비중을 `SLSQP` 기법으로 찾습니다.
- `generate_efficient_frontier`는 목표 기대수익률을 바꾸며 최적 포트폴리오의 위험-수익 트레이드오프 곡선을 생성합니다.
- `main` 함수는 샘플 기대수익률과 공분산 행렬을 사용해 효율적 프론티어를 시각화합니다.

# Chapter 4. Real World & Critical View (한계와 비평)

마코위츠 MPT는 절대적인 해답이라기보다는 투자 위험-수익 간의 균형 발견을 위한 훌륭한 출발점입니다. 그럼에도 실무와 현실 시장에서 다음과 같은 한계가 명확히 드러납니다.

1. **입력값의 불확실성**  
   평균 수익률과 공분산 행렬은 과거 데이터에 근거해 추정되는데, 이는 극히 불확실하며 작은 추정 오차가 최적화 결과에 큰 변화를 초래합니다. 특히 기대수익률은 노이즈에 매우 민감하여 과대평가 또는 과소평가의 결과를 낳기 쉽습니다.

2. **정적 모델**  
   마코위츠 이론은 특정 기간 수익률 통계에 기반하며, 시시각각 변하는 시장 상황이나 투자자의 위험선호 변동을 반영하지 못합니다. 즉, 동적인 시장 환경에 대한 적응력이 부족합니다.

3. **단순 리스크 척도**  
   변동성(분산)만을 리스크로 간주하는 것은 왜곡된 평가일 수 있습니다. 예컨대, 큰 폭 손실에 민감한 투자자들은 리스크를 '하방 위험'(downside risk)으로 측정하는 것이 더 적합합니다.

4. **투자 제한 미반영**  
   실무에서는 공매도 금지, 거래 비용, 세금, 최소투자 단위, 유동성 제약 등의 복잡한 규정이 존재합니다. MPT는 대부분 이들을 고려하지 않거나 단순화합니다.

5. **과거 데이터 의존성**  
   LTCM 파산이나 2008년 글로벌 금융위기는 MPT 기반 전략의 취약성을 여실히 보여줬습니다. 시장 충격 시 자산 간 상관관계가 급격히 변하거나, 비정상적인 이벤트가 발생해 모델이 예측치 못하는 결과를 초래합니다.

### 실무 적용 시의 Practice Tip

- **강건 최적화(Robust Optimization)** 기법을 도입해 입력 변수의 불확실성을 반영하는 방법을 권장합니다.
- **평균-분산 외 리스크 척도**(예: Conditional Value at Risk(CVaR), 하방 위험)를 함께 고려하는 하이브리드 모델을 사용합니다.
- 시장 데이터의 실시간 변화를 반영하는 **동적 자산배분 전략**과 머신러닝 기법을 병행합니다.
- 거래비용과 공매도 제약 등을 반영한 **제약조건 강화** 및 **스파스성(sparsity) 제약**을 추가하여 실용성을 높입니다.

마코위츠 MPT는 금융공학 발전의 토대이며, 이를 정확히 이해하고 Python 구현 능력을 배양하는 것은 퀀트 투자자에게 필수적인 자산관리 역량입니다. 그러나 이론의 한계를 인지하고 최신 기법과 융합해 나아가는 비판적 시야가 더욱 중요합니다.