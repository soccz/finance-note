# Chapter 1. The Theoretical Origins (이론적 기원)

현대 자산가격결정 이론 중 가장 널리 알려졌던 모델인 자본자산가격모형(CAPM, Capital Asset Pricing Model)은 1960년대 초 윌리엄 샤프(William Sharpe), 존 린트너(John Lintner), 잭 트레노(Jack Treynor) 등 여러 학자에 의해 독립적으로 개발되었습니다. CAPM의 등장은 1950년대 해리 마코위츠(Harry Markowitz)의 포트폴리오 이론이 확립된 이후 이루어졌다는 점에서 지극히 자연스러운 발전 단계였습니다.

마코위츠는 “리스크와 수익의 트레이드오프”를 수학적으로 명확히 정의하였고, 이를 통해 위험 분산의 중요성을 최초로 체계화했습니다. 그러나 그의 이론은 다수 자산으로 구성된 최적 포트폴리오를 생성하는 데 집중했던 반면, 개별 자산의 기대수익률을 결정하는 ‘시장 메커니즘’에 대한 이론적 해답은 없었습니다. 이에 대해 CAPM은 “어떤 자산이 합리적 투자자에게 요구되는 초과수익률(expected excess return)을 결정하는가?”라는 질문에 대한 최초의 엄밀한 답변을 시도했습니다.

기본 가정은 다음과 같습니다.  
- 투자자는 평균-분산 효용(maximizing mean-variance utility)을 극대화하며, 투자 기간은 단일 기간(single-period)으로 한정된다.  
- 시장은 완전경쟁이며, 모든 투자자는 동일한 기대수익률과 분산-공분산 리스크를 가진 자산에 대한 동일한 판단을 내린다.  
- 무위험 자산이 존재하며, 동일한 차입 및 대출이 가능하다.  
- 자산은 유동적이며, 모든 투자자는 포트폴리오 가중치를 조정할 수 있다.

CAPM의 핵심 아이디어는 “시장 포트폴리오에 대한 개별 자산의 관계, 즉 베타 계수(beta)가 자산의 체계적 위험을 나타내며, 이를 통해 개별 자산의 기대수익률이 결정된다”는 점입니다. 이것은 마코위츠의 분산-공분산 행렬이 구체적인 기대수익률 결정 모델로 확장됨으로써, 가격 결정의 균형 상태를 정량적으로 표현할 수 있다는 혁신적인 접근입니다.

CAPM 모델의 등장 이전 자산가격 결정은 경험적 규칙(rule of thumb)이나 단순 수익률 비교에 의존했으며, 효율적인 리스크 측정 및 투자 보상 구조에 대한 명확한 해석이 부족했습니다. CAPM은 이러한 빈틈을 메우며, 금융공학의 학문적 기반을 공고히 하는 데 크게 기여했습니다.

---

# Chapter 2. Mathematical Rigor (수학적 증명)

CAPM의 수학적 전개는 주로 다음과 같은 과정으로 구성됩니다.  

우선 $N$ 개의 위험자산이 존재하고, 해당 자산들의 수익률 벡터를  
$$ \mathbf{R} = (R_1, R_2, \dots, R_N)^\top $$  
라 정의합니다. 기대수익률과 공분산 행렬은  
$$ \mathbf{\mu} = \mathbb{E}[\mathbf{R}], \quad \mathbf{\Sigma} = \mathrm{Cov}[\mathbf{R}] $$  
라고 하겠습니다.  

투자자는 포트폴리오 가중치 벡터  
$$ \mathbf{w} = (w_1, w_2, \dots, w_N)^\top $$  
를 선택하며, 전체 가중치의 합은 1입니다.  

투자자의 문제는 위험회피 정도 $\lambda$에 대해 평균과 분산을 동시에 고려한 효용함수  
$$ U(\mathbf{w}) = \mathbf{w}^\top \mathbf{\mu} - \frac{\lambda}{2} \mathbf{w}^\top \mathbf{\Sigma} \mathbf{w} $$  
를 최대로 하는 $\mathbf{w}$를 찾는 문제입니다.

이를 위해 라그랑주 승수법을 도입하여, 제약조건  
$$ \mathbf{w}^\top \mathbf{1} = 1 $$  
하에서  
$$ \mathcal{L} = \mathbf{w}^\top \mathbf{\mu} - \frac{\lambda}{2} \mathbf{w}^\top \mathbf{\Sigma} \mathbf{w} - \gamma (\mathbf{w}^\top \mathbf{1} - 1) $$  
를 최적화합니다.

편미분 조건에서,  
$$ \frac{\partial \mathcal{L}}{\partial \mathbf{w}} = \mathbf{\mu} - \lambda \mathbf{\Sigma} \mathbf{w} - \gamma \mathbf{1} = \mathbf{0} $$  
로부터,  
$$ \mathbf{w} = \frac{1}{\lambda} \mathbf{\Sigma}^{-1} (\mathbf{\mu} - \gamma \mathbf{1}) $$

여기서 $\gamma$는 $\mathbf{w}^\top \mathbf{1} = 1$을 만족하도록 조정됩니다.

무위험 자산이 존재하고, 시장 전체 포트폴리오 $\mathbf{w}^*$가 존재한다면, CAPM은 모든 위험자산 수익률이 시장 수익률 $R_m$의 선형 조합임을 보여줍니다. 즉, 임의의 자산 $i$의 기대수익률은  
$$ \mathbb{E}[R_i] = R_f + \beta_i (\mathbb{E}[R_m] - R_f) $$  
이고, 베타 $\beta_i$는  
$$ \beta_i = \frac{\mathrm{Cov}(R_i, R_m)}{\mathrm{Var}(R_m)} $$  
입니다.

이때 라그랑주 승수법과 행렬 대수 기반으로 구성된 증명 과정은,  
- 마코위츠 포트폴리오 최적화가 언제나 존재함  
- 시장균형하에서 모든 투자자의 최적 포트폴리오가 동일한 시장 포트폴리오에 집중됨  
- 균형 조건으로부터 자산별 초과수익률이 베타에 의존함  

을 수학적 엄밀성으로 증명합니다.

---

# Chapter 3. Python Implementation (구현)

아래 코드는 실제 금융 시계열 데이터로부터 자산과 시장 포트폴리오의 과거 수익률을 통해 Rolling Beta를 계산하는 예제입니다. 통상적으로 60일 이동창(window)으로 베타를 산출하며, pandas와 statsmodels, matplotlib을 사용합니다.  

```python
import numpy as np
import pandas as pd
import statsmodels.api as sm
import matplotlib.pyplot as plt
from typing import Tuple

def calculate_rolling_beta(
    asset_returns: pd.Series,
    market_returns: pd.Series,
    window: int = 60
) -> pd.Series:
    """
    주어진 자산 및 시장 수익률에 대해 Rolling Beta를 계산합니다.

    Parameters
    ----------
    asset_returns : pd.Series
        자산의 일별 수익률 시계열
    market_returns : pd.Series
        시장포트폴리오의 일별 수익률 시계열
    window : int, optional
        Rolling 계산에 사용할 기간, 기본값은 60

    Returns
    -------
    pd.Series
        각 시점별 Rolling Beta 값 (window 기간 이후부터 계산 가능)
    """
    betas = []

    for start in range(len(asset_returns) - window + 1):
        y = asset_returns.iloc[start : start + window]
        x = market_returns.iloc[start : start + window]
        x = sm.add_constant(x)  # 절편 추가
        model = sm.OLS(y, x).fit()
        beta = model.params[1]  # 시장 수익률의 계수
        betas.append(beta)

    # Rolling Beta 시리즈 생성: 첫 (window - 1)개는 NaN
    beta_series = pd.Series(
        [np.nan] * (window - 1) + betas,
        index=asset_returns.index
    )

    return beta_series

def plot_rolling_beta(beta_series: pd.Series, asset_name: str) -> None:
    """
    계산된 Rolling Beta를 시각화합니다.

    Parameters
    ----------
    beta_series : pd.Series
        Rolling Beta 시계열
    asset_name : str
        자산 이름
    """
    plt.figure(figsize=(12, 6))
    plt.plot(beta_series, label=f'Rolling Beta ({asset_name})')
    plt.axhline(1.0, color='red', linestyle='--', label='Beta = 1')
    plt.title(f'Rolling Beta Over Time for {asset_name}')
    plt.xlabel('Date')
    plt.ylabel('Beta')
    plt.legend()
    plt.grid(True)
    plt.show()

# 예시 데이터 로딩 (CSV 형식, DateIndex, 수익률 컬럼명 일치 필요)
# asset_returns = pd.read_csv('asset_returns.csv', index_col=0, parse_dates=True)['Return']
# market_returns = pd.read_csv('market_returns.csv', index_col=0, parse_dates=True)['Return']

# beta_series = calculate_rolling_beta(asset_returns, market_returns, window=60)
# plot_rolling_beta(beta_series, asset_name='Sample Asset')
```

위 코드는 실제로 시계열 수익률 데이터가 있다면 바로 실행 가능하며, 다양한 자산의 베타 값이 시간에 따라 어떻게 변화하는지 시각 통해 심도 있게 분석할 수 있습니다. 이는 CAPM의 베타가 정적이지 않고 시장 상황에 따라 변화함을 비판하는 현대 연구와도 직접적으로 연계됩니다.

---

# Chapter 4. Real World & Critical View (한계와 비평)

CAPM은 금융 이론 발전에 지대한 공헌을 했음에도 불구하고, 실제 자본시장 데이터에서는 여러 한계와 실패 사례들이 발견되어 왔습니다.

첫째, CAPM이 제시한 베타는 단일 요인에 의존하며, 실제 자산 수익률의 움직임은 이를 훨씬 능가하는 여러 요인(ex. 규모 요인, 가치 요인, 모멘텀 등)에 의해 결정되는 경향이 확인되었습니다. 특히 1990년대以降 Fama와 French의 3-요인 모형, Carhart의 4-요인 모형과 같은 대안적 모델들이 발표되면서 “Beta is Dead” 논쟁이 활발해졌습니다.

둘째, 롱텀 캐피탈 매니지먼트(LTCM) 사례(1998년)와 2008년 금융 위기는 CAPM 기반 모델들의 정상시장 가정(정상분포, 독립, 균형상태)이 붕괴될 경우 그 취약점이 치명적으로 드러난다는 사실을 보여줬습니다. 시장 충격시 베타 값이 급격히 변화하거나, 오히려 시장 전반과 역상관되는 움직임이 나타나 예측력 상실로 이어지곤 했습니다.

셋째, CAPM의 수학적 가정 중 무위험 이자율이 동일하게 모든 투자자에게 적용되며, 차입 및 대출에 제약이 없다는 현실과 동떨어진 가정도 문제입니다. 복잡한 시장 마찰, 자산 비유동성, 정보 비대칭 등이 주가 결정에 핵심적 역할을 하기에, CAPM은 이들을 반영하지 못했습니다.

실무에서는 이러한 한계를 극복하기 위해 다음과 같은 조치들이 일반적입니다.  
- 베타 산출 방법에 다양한 가중치를 부여하여 최근 시장 변동에 민감하도록 가중 이동평균 사용  
- 다요인 모형으로 확장해 다중 리스크 프리미엄(예: 가치, 성장, 변동성 요인) 포함  
- 비선형 및 머신러닝 알고리즘 도입으로 비정상적 리스크 구조 탐색  
- 스트레스 테스트 및 시나리오 분석으로 위기 상황 대비 강화

따라서 CAPM은 자산 가격 결정 이론의 출발점으로서 가치와 의미를 지니지만, 이를 절대적 진리로 받아들여 다각적인 시장 분석을 소홀히 하는 것은 위험합니다. 본 장에서 다룬 Rolling Beta 계산 예시 또한 “변하는 리스크” 개념을 이해하고자 하는 시도이며, 이를 바탕으로 더 정교한 모델 구축이 활발히 진행되고 있음을 인지하는 것이 중요합니다.

---

# 결론

본 강의 노트에서는 현대 자산가격결정 모델의 시작점인 CAPM에 대해 그 역사적 배경, 수학적 엄밀성, 구현 방법, 그리고 현실 세계에서의 한계까지 심도 있게 다루었습니다. 퀀트 투자자와 연구자라면, CAPM 자체를 넘어, 베타 계수의 역할과 한계를 폭넓게 이해하고 최신의 다요인 모델, 데이터 과학 기술과 결합하여 보다 실용적인 자산 포트폴리오와 위험관리 전략을 수립해야 할 것입니다.

---

# 참고문헌
- Sharpe, W. F. (1964). Capital Asset Prices: A Theory of Market Equilibrium under Conditions of Risk. *The Journal of Finance*.  
- Fama, E. F. & French, K. R. (1993). Common risk factors in the returns on stocks and bonds. *Journal of Financial Economics*.  
- Roll, R. (1977). A Critique of the Asset Pricing Theory’s Tests. *Journal of Financial Economics*.  
- Ang, A., Hodrick, R. J., Xing, Y., & Zhang, X. (2006). The Cross-Section of Volatility and Expected Returns. *The Journal of Finance*.  

(끝)