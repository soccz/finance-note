# 평균-분산(Mean-Variance) 프레임워크 심층 강의 노트  
**일시**: 2026-01-30 (1주차 4일차)  
**주제**: 불확실성과 효용 (Utility Theory) – 평균-분산 프레임워크  
**대상**: 컴퓨터 공학 배경 퀀트 지망생  
---  

## Chapter 1. The Theoretical Origins (이론적 기원)  

1950년대 초, 금융은 매우 정성적이고 직관적인 투자 판단에 의존하던 시대였습니다. 당시의 금융시장에서 투자자들은 ‘어떤 자산을 얼마나 보유해야 하는가’라는 문제에 대하여 명확한 수학적 답변을 제공할 수 없었습니다. 해리 마코위츠(Harry Markowitz)는 1952년 <Portfolio Selection>이라는 획기적인 논문을 발표하며, 위험과 수익의 균형을 정량적으로 다룰 수 있는 새로운 패러다임을 제시하였습니다.  

마코위츠가 당시 해결하고자 했던 핵심 문제는 다음과 같습니다. 투자자의 관점에서 “최대한의 기대수익(expected return)을 확보하되, 동시에 위험(risk)을 최소화하는 포트폴리오 구성은 무엇인가?”였습니다. 기존에는 개별 자산의 기대수익률만 고려하거나, 단순한 직관에 따라 자산을 배분했지만, 이러한 접근은 분산투자 효과를 체계적으로 반영하지 못했습니다.  

마코위츠는 포트폴리오의 위험을 단순히 각 자산의 개별 위험의 합으로 보지 않고, 자산 간 상관관계(공분산)를 고려한 ‘포트폴리오 수익률의 분산’으로 정의합니다. 당시 수학계에서 공분산행렬을 적극적으로 활용한 최적화 문제는 신선한 접근이었습니다. 이는 물리학에서 열 확산 방정식, 공학에서 제어 이론 등의 외부 분야에서 사용되던 수학적 도구를 금융에 도입한 혁신이었습니다.  

마코위츠의 접근은 투자 이론의 패러다임 변화를 촉발해, 노벨 경제학상 수상(1990년)을 받으며 금융공학, 자산관리, 그리고 경제학 전반에 지대한 영향을 미쳤습니다. 이후 윌리엄 샤프(William Sharpe)의 자본자산가격결정모형(CAPM)도 이 프레임워크를 기반으로 발전했습니다.  

이처럼 마코위츠 평균-분산 이론은 “위험과 수익의 트레이드오프(trade-off)를 수학적 최적화로 다룰 수 있다”라는 금융 이론사에 혁신적 전환점이 된 것입니다. 독자는 이 맥락에서 평균-분산 프레임워크의 탄생 배경과 필요성을 직관적으로 이해할 수 있어야 합니다.  

---

## Chapter 2. Mathematical Rigor (수학적 증명)  

평균-분산 프레임워크는 기본적으로 다음과 같은 문제를 최적화하는 것에서 출발합니다.  

투자 대상 자산이 $N$개 있고, $R = (R_1, R_2, ..., R_N)^T$를 각각의 자산 수익률 확률변수 벡터라고 할 때,  
- 기댓값 벡터:  
$$
\boldsymbol{\mu} = \mathbf{E}[R] = 
\begin{bmatrix}
\mathbf{E}[R_1] \\
\mathbf{E}[R_2] \\
\vdots \\
\mathbf{E}[R_N]
\end{bmatrix}
$$  
- 공분산 행렬:  
$$
\Sigma = \mathbf{Cov}[R] = 
\begin{bmatrix}
\sigma_{11} & \sigma_{12} & \cdots & \sigma_{1N} \\
\sigma_{21} & \sigma_{22} & \cdots & \sigma_{2N} \\
\vdots & \vdots & \ddots & \vdots \\
\sigma_{N1} & \sigma_{N2} & \cdots & \sigma_{NN}
\end{bmatrix}
$$  
여기서 $\sigma_{ij} = \mathbf{Cov}[R_i, R_j]$이며, $\Sigma$는 대칭이고 양의 준정부호(positive semidefinite)임에 유의하십시오.  

투자자의 포트폴리오 가중치를 $\mathbf{w} = (w_1, ..., w_N)^T$로 정의하며,  
- $w_i$는 자산 $i$에 할당된 비중 (완전 투자시 $\sum_{i=1}^N w_i = 1$)  
- 이러한 가중치 적용 시 포트폴리오 수익률은 확률변수로  
$$
R_p = \mathbf{w}^T R = \sum_{i=1}^N w_i R_i
$$  
포트폴리오 기대 수익률과 분산은 다음과 같이 계산됩니다.  
$$
\mu_p = \mathbf{E}[R_p] = \mathbf{w}^T \boldsymbol{\mu}
$$  
$$
\sigma_p^2 = \mathbf{Var}[R_p] = \mathbf{w}^T \Sigma \mathbf{w}
$$  

마코위츠는 투자자의 위험 선호도를 이용해 다음 최적화 문제를 제시합니다. 투자자의 목표는 목표 기대수익률 $\mu^*$를 달성하면서 최소 분산을 찾는 것이거나, 반대로 위험 허용도에 따라 최대 수익을 찾는 문제로 나눌 수 있습니다. 대표적 문제 형식은,  
$$
\min_{\mathbf{w}} \quad \mathbf{w}^T \Sigma \mathbf{w}
$$  
subject to  
$$
\mathbf{w}^T \boldsymbol{\mu} = \mu^*, \quad \mathbf{w}^T \mathbf{1} = 1
$$  
여기서 $\mathbf{1}$은 모든 원소가 1인 $N$차원 벡터입니다.  

이 문제는 `Quadratic Programming`(이차 계획법)에 해당하며, 제약은 선형입니다. 라그랑주 승수법(Lagrange multiplier)를 사용해 해를 구해보겠습니다.  

라그랑지안은  
$$
\mathcal{L}(\mathbf{w}, \lambda, \gamma) = \mathbf{w}^T \Sigma \mathbf{w} - \lambda (\mathbf{w}^T \boldsymbol{\mu} - \mu^*) - \gamma (\mathbf{w}^T \mathbf{1} - 1)
$$  
이때  
$\frac{\partial \mathcal{L}}{\partial \mathbf{w}} = 0$  
를 풀면  
$$
2 \Sigma \mathbf{w} - \lambda \boldsymbol{\mu} - \gamma \mathbf{1} = 0
$$  
즉,  
$$
\mathbf{w} = \frac{1}{2} \Sigma^{-1} (\lambda \boldsymbol{\mu} + \gamma \mathbf{1})
$$  

이 식을 위 제약조건에 대입하여 $\lambda$, $\gamma$를 결정하면,  
$$
\begin{cases}
\mathbf{w}^T \boldsymbol{\mu} = \mu^* \\
\mathbf{w}^T \mathbf{1} = 1
\end{cases}
$$  
다음 연립방정식을 갖게 됩니다.  

함수 $A, B, C$를 정의하면,  
$$
A = \mathbf{1}^T \Sigma^{-1} \mathbf{1}, \quad B = \mathbf{1}^T \Sigma^{-1} \boldsymbol{\mu}, \quad C = \boldsymbol{\mu}^T \Sigma^{-1} \boldsymbol{\mu}
$$  

라그랑지 승수는  
$$
\begin{bmatrix}
\lambda \\
\gamma
\end{bmatrix}
= 2 \frac{1}{AC - B^2}
\begin{bmatrix}
A \mu^* - B \\
C - B \mu^*
\end{bmatrix}
$$  

결과적으로 최적 포트폴리오 가중치는  
$$
\mathbf{w}^* = \Sigma^{-1} \left[\frac{C - B \mu^*}{AC - B^2} \mathbf{1} + \frac{A \mu^* - B}{AC - B^2} \boldsymbol{\mu} \right]
$$  

이는 평균-분산 최적화의 핵심 해입니다. 여러 $\mu^*$에 대응하는 $\mathbf{w}^*$를 구하면 효율적 프론티어(efficient frontier)를 형성하며, 이는 투자자가 선택할 수 있는 위험 대비 최적 기대수익의 경계선입니다.  

---

## Chapter 3. Python Implementation (구현)  

이제 위 수식을 바탕으로 실제 포트폴리오 평균과 분산, 그리고 평균-분산 최적화를 구현해보겠습니다. Python의 과학계산 라이브러리를 활용하며, 실전 데이터셋도 활용 가능합니다.  

```python
import numpy as np
import pandas as pd
from scipy.optimize import minimize
import matplotlib.pyplot as plt

def portfolio_stats(weights: np.ndarray, mean_returns: np.ndarray, cov_matrix: np.ndarray) -> tuple:
    """
    포트폴리오의 기대수익과 분산(표준편차)를 계산합니다.
    
    :param weights: np.ndarray, 자산별 투자 비중 (합 1)
    :param mean_returns: np.ndarray, 자산별 기대수익률
    :param cov_matrix: np.ndarray, 자산 수익률 공분산 행렬
    :return: tuple (포트폴리오 기대수익, 표준편차)
    """
    portfolio_return = np.dot(weights, mean_returns)  # 가중평균 수익률
    portfolio_std = np.sqrt(np.dot(weights.T, np.dot(cov_matrix, weights)))  # 표준편차 = 분산의 제곱근
    return portfolio_return, portfolio_std

def minimize_variance(weights: np.ndarray, mean_returns: np.ndarray, cov_matrix: np.ndarray, target_return: float) -> dict:
    """
    목표 기대수익률을 충족하는 최소 분산 포트폴리오를 구합니다.
    
    :param weights: 초기 가중치 (초기값)
    :param mean_returns: 자산 별 기대수익률
    :param cov_matrix: 공분산 행렬
    :param target_return: 목표 기대수익률
    :return: 최적화 결과
    """
    n = len(mean_returns)
    
    # 목적함수: 포트폴리오 분산 최소화
    def portfolio_variance(w):
        return np.dot(w.T, np.dot(cov_matrix, w))
    
    # 가중치 합 = 1 제약
    constraints = (
        {'type': 'eq', 'fun': lambda w: np.sum(w) - 1},
        {'type': 'eq', 'fun': lambda w: np.dot(w, mean_returns) - target_return}
    )
    
    # 모든 자산에 대해 가중치 >= 0 (비단기매수 제한)
    bounds = tuple((0, 1) for _ in range(n))
    
    result = minimize(portfolio_variance, weights, method='SLSQP', bounds=bounds, constraints=constraints)
    return result

def efficient_frontier(mean_returns: np.ndarray, cov_matrix: np.ndarray, returns_range: np.ndarray) -> tuple:
    """
    효율적 프론티어를 계산하는 함수.

    :param mean_returns: 자산 평균 수익률
    :param cov_matrix: 공분산 행렬
    :param returns_range: 다양한 목표 기대수익률 범위
    :return: (risks, returns, weights_list)
    """
    n = len(mean_returns)
    weights_init = np.ones(n) / n  # 초기값: 균등배분
    risks = []
    returns = []
    weights_list = []
    
    for target_return in returns_range:
        opt = minimize_variance(weights_init, mean_returns, cov_matrix, target_return)
        if opt.success:
            ret, risk = portfolio_stats(opt.x, mean_returns, cov_matrix)
            risks.append(risk)
            returns.append(ret)
            weights_list.append(opt.x)
        else:
            risks.append(np.nan)
            returns.append(np.nan)
            weights_list.append(None)
    return np.array(risks), np.array(returns), weights_list

# --- 예제 데이터 생성 및 실행 ---
np.random.seed(42)
num_assets = 4

# 임의의 기대수익률과 공분산 행렬 정의
mean_returns = np.array([0.1, 0.12, 0.14, 0.09])
cov_matrix = np.array([
    [0.005, -0.010, 0.004, -0.002],
    [-0.010, 0.040, -0.002, 0.004],
    [0.004, -0.002, 0.023, 0.002],
    [-0.002, 0.004, 0.002, 0.010]
])

# 목표 기대수익률 범위 설정
target_returns = np.linspace(mean_returns.min(), mean_returns.max(), 100)

# 효율적 프론티어 계산
risks, returns, weights = efficient_frontier(mean_returns, cov_matrix, target_returns)

# 결과 시각화
plt.figure(figsize=(10, 6))
plt.plot(risks, returns, 'b--', label='Efficient Frontier')
plt.xlabel('Risk (Standard Deviation)')
plt.ylabel('Expected Return')
plt.title('Mean-Variance Efficient Frontier')
plt.legend()
plt.grid(True)
plt.show()
```

### 코드 설명  
- `portfolio_stats`: 주어진 가중치 벡터에 대해 포트폴리오 기대수익과 표준편차를 계산합니다.  
- `minimize_variance`: 목표 기대수익률을 만족시키면서 포트폴리오 분산을 최소화하는 최적의 가중치를 scipy.optimize의 SLSQP 방법으로 찾습니다.  
- `efficient_frontier`: 목표 기대수익률 구간을 설정하여 각 수익률에 해당하는 최소분산 포트폴리오를 각각 찾아서 효율적 프론티어를 그립니다.  
- 예제에서는 임의 데이터를 사용했으며, numpy 배열로 계산하므로 벡터화 및 행렬 연산이 효율적입니다.  

복잡한 현실 데이터에서는 `pandas.DataFrame`으로 여러 자산의 시계열 수익률 데이터를 관리하고, 공분산 행렬을 `df.cov()` 메서드로 구할 수 있습니다.  

---

## Chapter 4. Real World & Critical View (한계와 비평)  

평균-분산 모델은 투자론의 근간을 이루는 혁신이지만, 현실 시장에서는 여러 한계로 인해 비판받고 있습니다.  

### 1. 리스크 측정의 편협성  
평균-분산 프레임워크는 위험을 ‘분산’ 혹은 ‘표준편차’로만 측정합니다. 하지만 실제 금융시장에서 손실의 비대칭성, 꼬리 위험(tail risk), 극단적 이벤트 등은 분산만으로 포착되기 어렵습니다. investment losses는 일반적으로 분포의 하측 꼬리에 몰려있고, 분산은 상승과 하락 변동성을 동일하게 평가합니다. 결과적으로, 투자자는 실제로는 더 큰 손실 가능성을 무시할 수 있습니다.  

### 2. 정규분포 가정 및 수익률 독립성의 한계  
마코위츠 모델은 자산 수익률이 정규분포를 따르며 독립적이라는 점을 가정하는 경향이 큽니다. 실제 주식, 채권, 옵션 등의 수익률은 비정규 분포(첨도, 왜도 존재)를 보이며, 자산 간 상관관계는 위기 시 급격히 상승하는 경향이 있습니다(상관 붕괴).  

### 3. 추정 에러 문제  
평균 $\boldsymbol{\mu}$와 공분산 $\Sigma$는 과거 데이터를 기반으로 추정됩니다. 하지만 이 추정값은 매우 불확실하며 작은 입력 값의 오차가 포트폴리오 구성에 큰 영향을 미칩니다. 특히, 역행렬 계산시 수치적 불안정성이 높아짐으로써 해가 불안정할 수 있습니다.  

### 4. 단기매수제한 및 거래비용 비반영  
실무에서는 단기매도(short selling)를 제한하거나, 거래비용 및 세금을 비용함수를 통해 고려해야 합니다. 평균-분산 모델은 이러한 현실적 제약을 다루지 않습니다.  

### 5. 금융위기에서의 실패 사례  
1998년 LTCM(롱텀캐피털매니지먼트) 파산은 위험을 공분산 행렬로만 측정하는 모델의 한계를 극명히 보여줬습니다. 월스트리트 투자은행 역시 2008년 금융위기 당시 이론에 과도한 신뢰를 두고 극단적 시장 상황을 예측하지 못했습니다.  

### 실무 팁 (Practice Tip)  
- **리스크 척도 다양화**: 변동성과 VaR(Value at Risk), CVaR(Conditional Value at Risk)를 병합해 다중 리스크 척도를 사용합니다.  
- **강건한 최적화(Robust Optimization)**: 평균과 공분산의 추정 에러를 고려한 로버스트 최적화를 적용합니다.  
- **블록 부트스트래핑(bootstrapping)**이나 베이지안 추정법으로 모수 추정 불확실성을 반영합니다.  
- **상관관계 동적 추정**: GARCH, DCC 모델을 통해 시간에 따라 변하는 공분산 행렬을 추정합니다.  
- **실제 투자 규제 반영**: 단기매도 제한, 거래비용, 최소거래단위 등을 제약조건에 추가하는 최적화 문제를 설정합니다.  

---

# 요약  

- 마코위츠 평균-분산 이론은 금융 투자에서 위험과 수익의 정량적 최적화라는 패러다임 전환을 이끌었으며, 오늘날 자산관리의 기초를 제공합니다.  
- 핵심은 포트폴리오 기대수익을 고정하고 분산(위험)을 최소화하는 투자 가중치를 계산하는 문제로 귀결되며, 라그랑주 승수법을 통해 해석적 해를 도출합니다.  
- Python 구현에서는 `numpy` 행렬연산과 `scipy.optimize` 기반의 수치 최적화를 통해 현실적인 문제 해결이 가능함을 보여줍니다.  
- 하지만 분산만으로는 투자위험을 충분히 설명하지 못하며, 추정 오차, 비정규분포, 거래제한 등 현실 제약을 수용하지 못하는 한계가 분명히 존재합니다.  
- 따라서 실무에서는 다중 리스크 지표, 강건 최적화, 동적 공분산 추정, 거래 비용 반영 등을 추가한 확장 모델로 보완하여 사용합니다.  

금융공학 퀀트로서 평균-분산 모델의 수학적 메커니즘과 실무적 맥락을 정책적으로 이해하는 것은 기초이자 출발선입니다. 앞으로 더 정교한 위험모델과 최적화 기법이 등장하겠지만, 근본 이론의 해석과 구현 능력은 반드시 갖추어야 할 핵심 역량입니다.  

---  

(강의에서 다룬 참고문헌)  
- Markowitz, H. (1952). Portfolio Selection. *The Journal of Finance*, 7(1), 77-91.  
- 강의 교재 1장: 평균-분산 이론  
- 제공된 강의 슬라이드 및 영상 자료  

---  
*이상으로 2026년 1월 30일, 1주차 4일차 커리큘럼 "평균-분산 프레임워크" 심층 강의 노트를 마칩니다.*