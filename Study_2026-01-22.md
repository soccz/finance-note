> [!info] 오늘의 학습 목표  
금융 시계열 분석에서 **ARIMA(자기회귀 이동평균 통합 모델)**의 원리와 수식을 이해하고, 정상성 개념과의 연계성을 익힙니다. 또한 ARIMA가 금융 데이터 예측에서 왜 핵심적인지 수학적 증명과 함께 살펴봅니다.

---

## 1. ARIMA 모델 개요  

- **A**: AutoRegressive (자기회귀) - 현재 값이 과거 p시점 값들의 선형결합으로 표현됨  
- **I**: Integrated (차분) - 데이터의 정상성을 위해 d차 차분 수행  
- **MA**: Moving Average (이동평균) - 과거 q시점의 오차를 선형결합으로 반영  

> [!tip] 직관적으로 이해하기  
시계열 데이터가 시계에 붙은 진자처럼 흔들리지만, 이전 위치들과 진자의 충격을 기반으로 미래의 움직임을 예측하는 것과 같습니다.

---

## 2. 정상성과 차분  

지난 월요일에 배운 정상성 정의를 떠올려봅시다. 정상성은 통계적 특성이 시간에 따라 변하지 않는 속성입니다.  
ARIMA 모델에서 데이터가 비정상(non-stationary)이라면 d차 차분을 통해 정상성을 확보합니다.

**차분 연산자 Δ**는 다음과 같이 정의됩니다.  

$$
\Delta X_t = X_t - X_{t-1}
$$  

여기서 X_t는 시계열의 t시점 관측치입니다.

---

## 3. ARIMA(p,d,q) 모델 수식  

차분 후 정상화된 시계열 Y_t는 다음과 같은 ARMA(p,q) 형태를 가집니다.  

$$
\phi_p(B) Y_t = \theta_q(B) \epsilon_t
$$  

- B는 시차 연산자(BX_t = X_{t-1})  
- $\phi_p(B) = 1 - \phi_1 B - \cdots - \phi_p B^p$ (AR다항식)  
- $\theta_q(B) = 1 + \theta_1 B + \cdots + \theta_q B^q$ (MA다항식)  
- $\epsilon_t$는 백색잡음(오차항)

**원 시계열과의 관계**는 다음과 같습니다.  

$$
X_t = \Delta^{-d} Y_t
$$  

---

<details>
<summary>▶️ 수식 증명 심화</summary>

ARIMA 모델의 핵심은 차분 연산자를 적절히 적용하여 비정상 시계열을 정상 시계열로 전환하는 것입니다.  

- 차분 연산자 Δ를 d번 반복 적용하면  

$$
\Delta^d X_t = (1 - B)^d X_t = Y_t
$$  

- ARMA(p,q)모델에 의해 Y_t가 다음을 만족:  

$$
\phi_p(B) Y_t = \theta_q(B) \epsilon_t
$$  

이로써 ARIMA(p,d,q) 모델 정의 완료.

</details>

---

## 4. 금융 시계열에서 ARIMA의 중요성  

- 금융 데이터는 비정상성을 띠는 경우가 많아 차분 과정이 불가피  
- 확률적 충격과 자기 상관성을 동시에 모델링 가능하여 정밀 예측에 유리  
- 정상성 개념과 통합하여 안정적 모델링 기반을 제공함  

---

## 오늘 당장 시도해볼 것

- 자신이 관심 있는 금융 시계열 데이터(주가, 환율 등)를 골라서 d차 차분 과정을 직접 해보세요.  
- 여러 차수 p, q를 조정하며 간단한 ARIMA 모델을 구성해 보시기 바랍니다.  

---

> [!info] 관련 학습  
이번 내용은 지난 주 정상성, 시차 개념과 직접 연결되며, 다음주에는 GARCH 모델 및 변동성 추정으로 진행됩니다.
