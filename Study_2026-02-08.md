# 마코위츠와 포트폴리오 이론 (MPT) : 대학원 심층 강의 노트

---

## Chapter 1. The Theoretical Origins (이론적 기원)

현대 포트폴리오 이론(Modern Portfolio Theory, MPT)은 해리 마코위츠(Harry Markowitz)가 1952년에 발표한 논문 「포트폴리오 선택(Portfolio Selection)」에서 그 기원을 찾습니다. 당시 금융시장은 투자위험을 정량적으로 평가하는 방법론이 미비했고, 단순히 개별 자산별 수익률과 변동성만을 비교하는 정도에 그쳤습니다. 하지만 마코위츠는 투자위험을 단순히 자산의 개별위험의 합으로 보지 않고, 자산 간 상호관계 즉, 공분산(covariance)을 고려하는 것이 필수임을 인식했습니다.

마코위츠가 공분산 행렬을 도입한 배경에는 투자자들이 자산별 위험뿐만 아니라 자산 간 분산이 어떻게 상쇄되거나 증폭되는지를 알고 싶어 한다는 점이 있었습니다. 투자라는 행위가 단순히 개별 자산의 선택이 아니라 여러 자산의 조합으로서 전체 위험을 최소화하는 문제임을 체계적으로 표현하려 한 것입니다. 이 과정에서 통계학과 선형대수학, 최적화 이론이 긴밀하게 융합되었습니다.

이 이론은 다음과 같은 근본적인 문제를 해결하려 했습니다. 즉, “어떻게 하면 기대 수익률을 일정 수준으로 유지하면서 위험(분산)을 최소화할 수 있을까?” 또는 반대로 “어떻게 하면 주어진 위험 수준에서 기대 수익률을 최대화할 수 있을까?”라는 투자 의사결정에 핵심적인 문제를 정식화하였습니다.

나아가 이러한 개념은 위험과 수익률이라는 금융의 두 축을 수학적으로 명확히 정의함으로써, 투자 포트폴리오 구성에서 감정적 판단 또는 경험에 의존하던 과거 방식을 합리적이고 객관적인 최적화 문제로 승화시켰습니다. 마코위츠의 접근법은, 투자의 불확실성을 다룰 때 확률론과 통계학을 접목하여 리스크 관리의 새 시대를 열었다는 점에서 현대 금융공학의 시초라 할 수 있습니다.

---

## Chapter 2. Mathematical Rigor (수학적 증명)

마코위츠 포트폴리오 이론의 핵심은 기대수익률과 분산이라는 두 변수의 최적화를 통해 위험 대비 효율적인 포트폴리오를 찾는 것입니다. 이를 위해 우선, $N$개의 자산으로 구성된 포트폴리오를 생각합니다.

- 자산별 기대수익률 벡터:  
  $$
  \boldsymbol{\mu} =
  \begin{bmatrix}
  \mu_1 \\
  \mu_2 \\
  \vdots \\
  \mu_N \\
  \end{bmatrix}
  $$
- 자산별 보유 비중 벡터(포트폴리오 가중치):  
  $$
  \boldsymbol{w} =
  \begin{bmatrix}
  w_1 \\
  w_2 \\
  \vdots \\
  w_N \\
  \end{bmatrix}
  $$
  여기서 전체 투자금액 대비 비중을 나타내며, 모든 $w_i$의 합은 1 (완전 투자)이다.

- 자산 수익률의 공분산 행렬:  
  $$
  \Sigma = 
  \begin{bmatrix}
  \sigma_{11} & \sigma_{12} & \cdots & \sigma_{1N} \\
  \sigma_{21} & \sigma_{22} & \cdots & \sigma_{2N} \\
  \vdots & \vdots & \ddots & \vdots \\
  \sigma_{N1} & \sigma_{N2} & \cdots & \sigma_{NN} \\
  \end{bmatrix}
  $$

<br>

- 포트폴리오의 기대수익률:  
  $$
  R_p = \boldsymbol{w}^T \boldsymbol{\mu} = \sum_{i=1}^{N} w_i \mu_i
  $$

- 포트폴리오의 분산(즉, 위험):  
  $$
  \sigma_p^2 = \boldsymbol{w}^T \Sigma \boldsymbol{w} = \sum_{i=1}^N \sum_{j=1}^N w_i w_j \sigma_{ij}
  $$

<br>

### 최적화 문제 설정

투자자는 특정 기대수익률 $R^*$를 달성하면서 위험을 최소화하고자 합니다. 따라서 다음과 같은 문제를 푼다.

$$
\min_{\boldsymbol{w}} \quad \boldsymbol{w}^T \Sigma \boldsymbol{w}
$$
subject to
$$
\boldsymbol{w}^T \boldsymbol{\mu} = R^*, \quad \sum_{i=1}^{N} w_i =1
$$

이 문제는 **제한조건이 있는 이차형 최적화 문제(Quadratic Programming)**로, Lagrange multiplier를 활용해서 해를 구합니다.

<br>

### 라그랑주 함수

라그랑주 승수 $\lambda$, $\gamma$를 도입하면,

$$
\mathcal{L}(\boldsymbol{w}, \lambda, \gamma) = \boldsymbol{w}^T \Sigma \boldsymbol{w} - \lambda ( \boldsymbol{w}^T \boldsymbol{\mu} - R^*) - \gamma (\boldsymbol{w}^T \boldsymbol{1} -1)
$$

여기서 $\boldsymbol{1}$은 모든 원소가 1인 $N$차원 벡터이다.

<br>

### 최적화 조건

라그랑주 함수에 대해 $\boldsymbol{w}$로 미분하고 0과 같게 두면,

$$
\frac{\partial \mathcal{L}}{\partial \boldsymbol{w}} = 2 \Sigma \boldsymbol{w} - \lambda \boldsymbol{\mu} - \gamma \boldsymbol{1} = \boldsymbol{0}
$$

이는

$$
2 \Sigma \boldsymbol{w} = \lambda \boldsymbol{\mu} + \gamma \boldsymbol{1}
$$
를 의미한다.

$$
\Rightarrow \boldsymbol{w} = \frac{1}{2} \Sigma^{-1} ( \lambda \boldsymbol{\mu} + \gamma \boldsymbol{1})
$$

<br>

### 라그랑주 승수 구하기 위한 행렬식 계산

다음의 기저행렬과 벡터를 정의한다.

$$
A = \boldsymbol{1}^T \Sigma^{-1} \boldsymbol{1}, \quad B = \boldsymbol{1}^T \Sigma^{-1} \boldsymbol{\mu}, \quad C = \boldsymbol{\mu}^T \Sigma^{-1} \boldsymbol{\mu}
$$

$$
D = AC - B^2
$$

라그랑주 승수는 다음과 같이 구해진다.

$$
\lambda = \frac{2 (A R^* - B)}{D}, \quad \gamma = \frac{2 (C - B R^*)}{D}
$$

이를 $\boldsymbol{w}$에 대입하면 최적의 포트폴리오 가중치를 도출할 수 있다.

이 결과는 다음의 중요한 의미를 갖습니다.

- **효율적 프론티어(Efficient Frontier)**: 각 기대수익률 $R^*$에 대해 위험이 최소화되는 포트폴리오를 잇는 곡선
- **분산 최소화 포트폴리오(Minimum Variance Portfolio)**: 위험이 최저가 되는 포트폴리오
- **자본시장선(Capital Market Line)** (추가 모형에서 확장)

마코위츠 모델은 분산-기대수익률 공간에서 최적의 투자 선택을 체계적으로 제시하는 선구적인 수학적 구조를 확립하였습니다.

---

## Chapter 3. Python Implementation (구현)

아래 코드는 Python에서 `numpy`와 `scipy.optimize`를 활용하여, 주어진 자산 수익률과 공분산 행렬 데이터로부터 최적의 포트폴리오를 찾아내는 예제입니다. 특히 목표 기대수익률을 입력으로 받고, 위험(분산)을 최소화하는 가중치를 계산합니다.

```python
import numpy as np
import pandas as pd
from scipy.optimize import minimize
import matplotlib.pyplot as plt
from typing import Tuple

def portfolio_performance(weights: np.ndarray, mu: np.ndarray, cov: np.ndarray) -> Tuple[float, float]:
    """
    포트폴리오의 기대수익률과 분산을 계산합니다.
    
    Parameters
    ----------
    weights : np.ndarray
        포트폴리오 자산별 비중 (길이 N)
    mu : np.ndarray
        자산들의 기대수익률 벡터 (길이 N)
    cov : np.ndarray
        자산 수익률의 공분산 행렬 (NxN)
        
    Returns
    -------
    portfolio_return : float
        포트폴리오 기대수익률
    portfolio_variance : float
        포트폴리오 분산 (위험)
    """
    portfolio_return = np.dot(weights, mu)
    portfolio_variance = np.dot(weights, np.dot(cov, weights))
    return portfolio_return, portfolio_variance

def minimize_variance(mu: np.ndarray, cov: np.ndarray, target_return: float) -> np.ndarray:
    """
    목표 기대수익률을 만족시키면서 포트폴리오 분산을 최소화하는 가중치를 구합니다.
    
    Parameters
    ----------
    mu : np.ndarray
        자산들의 기대수익률 벡터
    cov : np.ndarray
        자산 수익률 공분산 행렬
    target_return : float
        목표 기대수익률
        
    Returns
    -------
    weights : np.ndarray
        최적화된 포트폴리오 가중치 벡터
    """
    num_assets = len(mu)
    args = (cov,)
    
    # 초기값 : 균등 가중치
    init_guess = np.ones(num_assets) / num_assets

    # 투자 비중의 합은 1이어야 함 (완전 투자)
    constraints = (
        {'type': 'eq', 'fun': lambda w: np.sum(w) - 1},
        {'type': 'eq', 'fun': lambda w: np.dot(w, mu) - target_return}
    )

    # 각 투자 비중은 0 이상 (단기매도 제한) - 필요하면 제거 가능
    bounds = tuple((0, 1) for _ in range(num_assets))

    # 목적함수: 포트폴리오 분산 최소화
    def portfolio_variance_fn(weights: np.ndarray, cov_matrix: np.ndarray) -> float:
        return np.dot(weights, np.dot(cov_matrix, weights))

    opt_result = minimize(
        portfolio_variance_fn,
        init_guess,
        args=args,
        method='SLSQP',
        bounds=bounds,
        constraints=constraints,
        options={'disp': False}
    )

    if not opt_result.success:
        raise ValueError("최적화 실패: " + opt_result.message)

    return opt_result.x

def example_run():
    """
    예제 실행: 세 자산에 대해 목표 수익률별 최적 분산 포트폴리오 계산 및 시각화
    """
    # 예제 자산 기대수익률 (연율화)
    mu = np.array([0.10, 0.12, 0.15])

    # 예제 자산 공분산 행렬
    cov = np.array([
        [0.005, -0.010, 0.004],
        [-0.010, 0.040, -0.002],
        [0.004, -0.002, 0.023]
    ])

    target_returns = np.linspace(0.10, 0.15, 50)
    port_variances = []

    weights_list = []

    for r in target_returns:
        w = minimize_variance(mu, cov, r)
        _, var = portfolio_performance(w, mu, cov)
        port_variances.append(var)
        weights_list.append(w)

    # 분산의 제곱근은 표준편차 (리스크)
    port_risks = np.sqrt(port_variances)

    # 효율적 프론티어 시각화
    plt.figure(figsize=(10, 6))
    plt.plot(port_risks, target_returns, 'b-', label='Efficient Frontier')
    plt.xlabel('Portfolio Risk (Standard Deviation)')
    plt.ylabel('Portfolio Expected Return')
    plt.title('Efficient Frontier: Minimum Variance Portfolio')
    plt.grid(True)
    plt.legend()
    plt.show()

if __name__ == "__main__":
    example_run()
```

### 코드 해설

- `portfolio_performance` 함수는 포트폴리오 가중치, 기대수익률 벡터, 공분산 행렬을 받아서 포트폴리오의 기대수익률과 분산을 계산합니다.
- `minimize_variance` 함수는 `scipy.optimize.minimize` 함수와 SLSQP 알고리즘을 이용해, 목표 기대수익률에 맞춰 분산을 최소화하는 가중치를 찾습니다.
- **제약조건**으로는 자산 가중치 합계가 1이어야 하고, 포트폴리오 기대수익률이 타깃 값과 일치해야 합니다.
- **범위 제약조건**으로 가중치를 0~1 사이로 두어 단기 매도(공매도)를 제한했습니다. (만약 공매도 허용 모델이면 bounds를 변경)
- `example_run` 함수는 3개 자산에 대해 목표 수익률을 바꾸며 최적 포트폴리오를 구하고, 효율적 프론티어 곡선을 시각화하는 예제입니다.

---

## Chapter 4. Real World & Critical View (한계와 비평)

마코위츠 MPT는 투자 의사결정을 체계적으로 수학화한 혁신적인 업적이지만, 현실 시장에서 다음과 같은 한계와 비판점을 받습니다.

### 1. 수익률 분포 가정의 문제

기본적으로 포트폴리오 위험을 분산(variance)으로 정의하는데 이는 수익률이 정규분포 또는 준정규(quasi-normal)를 따른다고 가정한 것입니다. 그러나 실제 금융자산 수익률은 꼬리가 두껍고(skewness, kurtosis 존재), 극단적인 사건(블랙스완)을 자주 포함합니다. 분산만으로 위험을 평가하면 극단위험을 과소평가 할 우려가 큽니다.

### 2. 공분산 행렬의 불안정성

포트폴리오 최적화의 핵심 매개변수인 공분산 행렬은 과거 수익률 데이터에 근거해 추정됩니다. 그러나 실제 시장에서는 공분산이 시간에 따라 크게 변동하고, 추정치의 오차가 최적화 결과를 심각하게 왜곡할 수 있습니다. 특히, 자산 수가 많아질수록 공분산 추정치의 불확실성 문제가 증폭됩니다.

### 3. 투자자 행동과 시장 동학의 불일치

MPT는 투자자가 수익률 기대와 분산을 통해 합리적 의사결정을 한다고 가정합니다. 하지만 투자자들은 종종 비합리적인 행동을 하고, 시장은 비효율적으로 움직이는 경우가 종종입니다. 또한 MPT는 가격 형성 메커니즘, 유동성 제약, 거래비용, 세금 등을 고려하지 않아 현실적 적용에 한계가 있습니다.

### 4. 단기 매도 및 레버리지 제한

현실에서는 공매도나 무제한 레버리지 활용이 정부 규제, 신용 위험, 마진 콜 등으로 제한되기 때문에, MPT의 최적해가 실제 적용할 수 없는 경우가 많습니다.

### 실무 적용 팁 (Practice Tips)

- **리스크 모형 확장**: 기대분산 모형 대신 VaR(Value at Risk), CVaR(Conditional VaR), 또는 비대칭 리스크 척도를 도입해 극단 위험을 반영합니다.
- **공분산 행렬 안정화**: 리샘플링, 팩터 모델, 또는 정규화 기법을 적용해 공분산 추정 오류를 줄입니다.
- **제약조건 강화**: 거래 비용, 유동성, 최소 투자단위, 공매도 제한 등 현실 제약조건을 추가해 실용성을 높입니다.
- **동적 포트폴리오 관리**: 정적 최적화 대신, 시장 변화에 대응하는 동적 포트폴리오 재조정 전략을 도입합니다.

### 역사적 실패 사례

- 1998년 LTCM(Long Term Capital Management) 파산: MPT, CAPM 등 전통 금융모델에 기반한 급진적 레버리지 투자가 러시아 디폴트 등 극단 이벤트에 취약함을 노출했습니다.
- 2008년 글로벌 금융위기: 자산가격 급락과 시장 급변동으로 인해 과거 수치 기반의 최적화 모델들이 과소 위험 평가 및 대규모 손실을 초래했습니다.

---

# 마무리

마코위츠 포트폴리오 이론은 금융공학과 투자관리의 근간을 이루는 불멸의 기초입니다. 오늘날 수학적 엄밀성과 함께 데이터 기반 최적화 및 프로그래밍 역량을 갖춘 퀀트 개발자에게는 반드시 숙달해야 할 핵심 모델입니다. 그러나 동시에 현실의 불확실성과 복잡성에는 항상 한계를 지니므로, 확장 모델과 실무 감각을 함께 갖추는 균형 잡힌 접근이 요구됩니다.

본 강의에서는 수학적 이론에서 Python을 활용한 계산까지 심층적으로 다루어, 금융공학자의 토대를 확고히 하는 데 일조하고자 합니다. 앞으로도 본 이론의 확장 및 응용편을 꾸준히 심화해 나갈 예정입니다.