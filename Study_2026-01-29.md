# Utility Theory - Day 3  
**[Python] Utility Function 시각화**  
2026-01-29 | 대학원 1주차 3일차 | Phase 1: 고전적 금융의 탄생

---

## Chapter 1. The Theoretical Origins (이론적 기원)

기대 효용 이론(Expected Utility Theory)은 현대 금융공학과 경제학의 토대 중 하나로, 투자자의 불확실성 하 의사결정을 설명하는 기본 도구입니다. 이 이론의 탄생 배경을 이해하려면 20세기 초 금융시장 분석에서 효용 개념이 왜 도입되었는지부터 살펴야 합니다.

1920~30년대, 투자자들은 수익률과 위험을 동시에 고려하는 의사결정의 ‘최적화 문제’를 고민하기 시작했습니다. 마코위츠(Harry Markowitz)의 포트폴리오 이론은 리스크(분산)를 줄이며 기대수익률을 극대화하는 수학적 틀을 제공하였고, 이는 투자자 행동의 객관화 시도였습니다. 하지만 포트폴리오 리스크를 분산으로만 표현하는 데엔 한계가 있었습니다. 투자자의 ‘주관적 선호’가 반영되지 못했기 때문입니다.

이에 프란시스 에지워스(Francis Edgeworth), 존 폰 노이만(John von Neumann), 오스카 모르겐슈테른(Oskar Morgenstern) 등이 1940년대에 기대 효용 이론을 체계화했습니다. 그들은 투자자의 ‘효용 함수’를 도입하여 수익률 그 자체가 아닌 ‘수익에 대한 만족도’를 통해 위험 선호도를 모델링하고자 했습니다. 즉, 단순히 수학적 기대값 대신 ‘기대 효용’을 극대화하는 방법으로 투자 결정을 설명하는 틀을 완성했습니다.

이렇게 효용 함수가 탄생한 배경에는 ‘투자의 사회적, 심리적 요인’ — 특히 위험 회피 성향을 수학적으로 분석하려는 강한 동기와 문제의식이 존재합니다. 이론적으로, 효용 함수는 투자자가 같은 기대수익률 내에서도 위험이 다르거나 결과의 분포가 다를 때 어떻게 행동하는가를 정량화하는 핵심 도구입니다. 

오늘날, 로그 효용, CRRA(Constant Relative Risk Aversion) 효용 등 다양한 함수 형태가 제안되어 왔고, 투자자마다 다르게 적용 가능한 ‘맞춤형’ 위험 성향 모델링이 가능합니다. Python 기반 시각화 작업을 통해 이러한 함수들의 속성과 투자자 행동의 직관적 이해가 가능해졌다는 점 또한 주목할 필요가 있습니다.

---

## Chapter 2. Mathematical Rigor (수학적 증명)

효용 이론에서 가장 핵심적인 것은 ‘기대 효용’ 개념입니다. 투자자가 가능한 결과의 확률분포 하에서 효용 함수 $U(W)$에 대한 기대값을 최대화하려 할 때, 효용 함수는 다음과 같은 성질을 가집니다.

$$
U: \mathbb{R}_+ \to \mathbb{R}, \quad U^\prime(W) > 0, \quad U^{\prime\prime}(W) < 0
$$

- $U'(W) > 0$: 부(wealth)가 증가할수록 효용도 증가 (투자자 선호의 정상성)
- $U''(W) < 0$: 효용함수의 오목성 (위험회피 성향)

여기서 투자자의 위험회피 정도는 ‘상대적 위험회피도(Relative Risk Aversion, RRA)’로 정의됩니다.

$$
RRA(W) = - \frac{W \cdot U''(W)}{U'(W)}
$$

통계학과 경제학에서 널리 쓰이는 대표적인 효용 함수는 다음과 같습니다.

### 1. 로그 효용 함수(Log Utility)

$$
U(W) = \ln(W), \quad W > 0
$$

로그 함수의 경우,

$$
U'(W) = \frac{1}{W}, \quad U''(W) = -\frac{1}{W^2}
$$

따라서 상대적 위험회피도는

$$
RRA(W) = - \frac{W \cdot (-\frac{1}{W^2})}{\frac{1}{W}} = 1
$$

즉, 로그 효용은 상대적 위험회피도가 1인 표준적 위험회피 투자자를 모델링합니다.

### 2. CRRA 효용 함수(Constant Relative Risk Aversion Utility)

일반형은 다음과 같습니다.

$$
U(W) = 
\begin{cases}
\frac{W^{1-\gamma}}{1 - \gamma}, & \gamma \neq 1 \\
\ln(W), & \gamma = 1
\end{cases}
$$

여기서 $\gamma > 0$는 상대적 위험회피도의 척도입니다.

도함수 계산시,

$$
U'(W) = W^{-\gamma}, \quad U''(W) = -\gamma W^{-\gamma -1}
$$

따라서

$$
RRA(W) = - \frac{W (-\gamma W^{-\gamma -1})}{W^{-\gamma}} = \gamma
$$

즉, CRRA 효용 함수는 상대적 위험회피도가 상수 $\gamma$임을 보장합니다. $\gamma > 1$이면 강한 위험회피, $\gamma < 1$이면 위험중립에 가깝습니다.

---

### 효용 최대화 문제 (일반형)

투자자가 효용을 최대화하는 투자 문제는 기대 효용 극대화 문제로 나타낼 수 있습니다.

$$
\max_{x} \; \mathbb{E}[U(W_T(x))]
$$

여기서 $x$는 투자 포트폴리오, $W_T$는 만기 시 자산가치입니다. $W_T(x)$는 확률변수이므로 기대값 내에 효용함수가 들어가 있습니다.

---

### 의사결정 이론적 도구

- **라그랑주 승수법(Lagrange Multiplier):** 효용 최대화 문제에 제약조건(예: 투자 총액 제한)이 있을 때 최적화 문제 해법으로 쓰임.
- **단계적 기대의 법칙:** 여러 시점 투자 결정에서 동적 프로그래밍 원리로 활용.
- **확률미분방정식, Ito's Lemma:** 연속 시계열 위험자산 가격 모델과 효용 극대화 문제 연계 시 필수적.

---

## Chapter 3. Python Implementation (구현)

본 절에서는 가장 대표적인 효용 함수인 로그 효용과 CRRA 효용을 Python을 활용해 구현하고, 위험 회피 계수 $\gamma$ 변화에 따른 함수 형태 변화를 시각화합니다. NumPy와 Matplotlib 라이브러리를 사용해 0~5의 부의 구간에서 효용값 변화를 직관적으로 파악할 수 있습니다.

```python
import numpy as np
import matplotlib.pyplot as plt
from typing import Callable

def log_utility(wealth: np.ndarray) -> np.ndarray:
    """
    로그 효용 함수.

    Args:
        wealth (np.ndarray): 부(wealth)의 배열, 양수 값이어야 함.

    Returns:
        np.ndarray: 각 부에 대한 효용 값.
    """
    if np.any(wealth <= 0):
        raise ValueError("Wealth must be strictly positive for log utility.")
    return np.log(wealth)

def crra_utility(wealth: np.ndarray, gamma: float) -> np.ndarray:
    """
    상수 상대 위험회피 (CRRA) 효용 함수.

    Args:
        wealth (np.ndarray): 부(wealth)의 배열, 양수 값이어야 함.
        gamma (float): 상대 위험회피 계수 (gamma != 1)

    Returns:
        np.ndarray: 각 부에 대한 효용 값.
    """
    if np.any(wealth <= 0):
        raise ValueError("Wealth must be strictly positive for CRRA utility.")
    if gamma == 1.0:
        return np.log(wealth)  # gamma=1일 때 로그효용과 동일
    else:
        return (wealth ** (1 - gamma)) / (1 - gamma)

def plot_utilities(wealth: np.ndarray, gammas: list[float]) -> None:
    """
    다양한 상대 위험회피 계수에 따른 CRRA 효용 함수 및 로그 효용 함수 시각화.

    Args:
        wealth (np.ndarray): 부(wealth) 값 범위
        gammas (list[float]): 상대 위험회피 계수 리스트
    """
    plt.figure(figsize=(10, 7))
    
    # 로그 효용 함수 (gamma=1)
    plt.plot(wealth, log_utility(wealth), label='Log Utility ($\\gamma=1$)', linewidth=2, color='black')

    # CRRA 함수들
    for gamma in gammas:
        if gamma != 1:
            util_vals = crra_utility(wealth, gamma)
            plt.plot(wealth, util_vals, label=f'CRRA ($\\gamma={gamma}$)')

    plt.title('Utility Function Visualization')
    plt.xlabel('Wealth')
    plt.ylabel('Utility')
    plt.legend()
    plt.grid(True)
    plt.show()


if __name__ == "__main__":
    wealth_range = np.linspace(0.01, 5, 500)  # 0이어서는 안됨(로그 계산 오류)
    gammas_to_plot = [0.5, 2, 5]  # 위험회피도 변화 예시

    plot_utilities(wealth_range, gammas_to_plot)
```

---

### 코드 설명

- `log_utility()` 함수는 부의 로그를 계산하며, 부가 0 이하일 경우 에러를 발생시켜 안정성을 높임.
- `crra_utility()` 함수는 파라미터 $\gamma$에 따라 효용 값을 계산하며, $\gamma=1$일 경우 로그 효용으로 자동 변환.
- `plot_utilities()` 함수에서는 여러 $\gamma$ 값에 대응하는 효용 함수 곡선을 한 그래프에 출력하여 위험회피도 변화가 효용 함수에 미치는 영향을 시각적으로 직관화함.
- Jupyter Notebook 환경에서 그대로 실행 가능하며, 투자자 효용 함수 형태 분석 및 교육용으로 적합.

---

## Chapter 4. Real World & Critical View (한계와 비평)

효용 이론은 금융경제학에 지대한 영향을 끼친 뛰어난 이론적 프레임이지만, 현실 시장에서 몇 가지 중요한 한계점과 비판점이 존재합니다.

### 1. 현실의 투자자 행동과 괴리

- 기대 효용 이론은 투자자가 위험에 대해 일관된 위험회피 계수를 가진다고 가정하나, 실질 투자자들은 감정, 심리편향, 인지 한계 등으로 이 가정을 위반합니다.
- 예컨대, ‘손실 회피(Loss Aversion)’ 및 ‘확률 왜곡’ 현상은 카니먼(Kahneman)과 트버스키(Tversky)의 행동경제학에서 밝혀졌는데, 이는 전통적 효용 함수가 포착하지 못하는 투자 행태를 설명합니다.

### 2. 분포 가정과 계산상의 어려움

- 고차원 포트폴리오에서는 정확한 수익률 분포 추정이 쉽지 않으며, 비정상 분포 조건하에서는 기대 효용 계산이 복잡합니다.
- 고도로 복잡한 자산군을 포함한 현실 시장에서는 기대 효용 극대화 문제 자체가 해석적으로 풀기 어려운 비선형 최적화가 됩니다.

### 3. 금융위기 사례 분석

- LTCM(Long-Term Capital Management)의 파산은 모델 기반 투자 전략이 비정상적 시장 상황에서 얼마나 치명적일 수 있는지를 증명합니다.
- 2008년 글로벌 금융위기는 시장 리스크가 표준 분산-효용 모델로는 평가 불가능한 ‘극단 리스크’를 내포하고 있음을 드러냈습니다. 효용 함수 하나로 ‘극단 상황’에서 투자자 행동을 예측하기에는 명확한 한계가 있습니다.

---

### 실무적 보완 및 적용 팁

- 투자자 효용 함수 매개변수(특히 $\gamma$)는 시장 상황과 투자 성향에 따라 동적으로 변할 수 있으므로 시계열 데이터에 기반해 주기적 재추정하는 것이 현실적입니다.
- 행동경제학적 요소를 반영한 Prospect Theory 기반 효용 함수 적용이 이루어지고 있으며, 이는 투자자 심리변수를 추가 모델링한 최신 트렌드입니다.
- ‘자산 가격의 위험 프리미엄’을 결정할 때 효용 함수와 결합된 자산 가격 이론(CAPM, APT 모델 등)을 활용하되, 과도한 확률 분포 가정에서 벗어난 ‘견고성 분석(Robustness analysis)’을 함께 수행해야 합니다.

---

## 참고자료

- Mas-Colell, A., Whinston, M.D., & Green, J. (1995). *Microeconomic Theory*. Chapter 6: Expected Utility Theory.
- Markowitz, H. (1952). Portfolio Selection. *Journal of Finance*.
- Kahneman, D., & Tversky, A. (1979). Prospect Theory: An Analysis of Decision under Risk. *Econometrica*.
- Lecture notes on Utility Theory (본 강의 자료).
- Python 3.x Documentation, Matplotlib & NumPy API Reference.

---

본 강의를 통해 기대 효용 이론의 이론적 배경과 수학적 엄밀성을 이해하고, Python을 활용한 실무적 시각화 기법을 습득하시기 바랍니다. 다음 강의에서는 투자자 효용 극대화 문제에 대한 최적화 기법과 연속 시간 모델에 적용하는 방법을 탐구할 것입니다.