# 대학원 심층 강의 노트  
## Phase 1: 고전적 금융의 탄생  
### Week 2, Day 9  
### 주제: 자본시장선(Capital Market Line, CML)과 무위험 자산  

---

## Chapter 1. The Theoretical Origins (이론적 기원)

현대 금융공학과 투자론의 토대가 된 마코위츠(Markowitz, 1952)의 포트폴리오 이론(MPT: Modern Portfolio Theory)은 위험과 수익률을 동시에 고려하여 자산 배분을 최적화하는 방법론을 제시하였습니다. 하지만 초기 마코위츠 이론은 **모든 자산이 위험을 내포하고 있다는 가정 아래 자산 간 수익률의 분산-공분산 행렬로 포트폴리오 위험을 최소화**하는 데 초점을 맞추었기 때문에, 실제 투자자가 직관적으로 사용하는 “무위험 자산”의 개념과는 완벽히 맞물리지 않았습니다.

---

### 왜 자본시장선(CML)이 등장했는가?

1950~60년대 초 미국 금융시장은 급속히 발전했으나, 마코위츠의 MPT만으로는 투자자들이 **가장 효율적인 투자조합**을 찾을 때 제한적이었습니다. 특히 다음과 같은 문제점이 있었습니다.

- 마코위츠 포트폴리오는 위험 자산만 고려하여 투자자들이 실제 톳톳한 투자 전략(예: 은행 예금, 국채 등 무위험 자산)을 활용할 수 없었다.
- 효율적 투자선상에서 개별 투자자마다 위험회피 정도에 따라 선택은 달랐으나, 시장 참여자가 무위험 자산을 투자대상에 슬쩍 투입할 수 있는 개념적 프레임워크가 미흡했다.
- 윌리엄 샤프(William Sharpe, 1964)는 이를 해결하기 위해 **무위험 자산을 포트폴리오에 도입하여 자본시장선(Capital Market Line, CML)** 이라는 개념을 정립하였고, 시장 전체의 위험자산과 무위험 자산의 조합으로 투자 효율성이 극대화된다는 점을 체계적으로 설명했습니다.

---

### CML과 MPT의 관계

샤프의 연구는 마코위츠의 MPT를 확장하는 의미였으며, 무위험 자산으로부터 시작해 모든 투자자가 같은 ‘시장 포트폴리오’를 최적 조합 포트폴리오로 고려한다는 점에서 **자본시장의 균형 상태(equilibrium)** 를 정의하였습니다. 이를 통해 위험자산뿐 아니라 무위험 자산을 포괄하는 자본배분이론이 완성되었으며, 이는 CAPM(자본자산가격결정모형) 이론체계의 근간이 됩니다.

---

## Chapter 2. Mathematical Rigor (수학적 증명)

---

### 2.1. 기본 개념과 문제 설정

- 무위험 자산의 기대수익률: \( R_f \) (예: 국채 수익률)
- 위험자산 포트폴리오 기대수익률: \( R_p \)
- 위험자산 포트폴리오의 표준편차(위험): \( \sigma_p \)
- 투자자의 전체 포트폴리오 기대수익률와 표준편차는 무위험 자산과 위험자산 포트폴리오의 조합으로 표현

---

### 2.2. 무위험 자산과 위험자산 조합의 수학적 표현

투자자는 전체 투자자금 중 \( x \)를 위험자산에, \( (1 - x) \)를 무위험 자산에 투자한다 했을 때, 기대수익률은 선형적으로 표현됩니다.

$$
R_C = R_f + x(R_p - R_f)
$$

하지만 위험은 무위험 자산이 위험이 없으므로,

$$
\sigma_C = x \sigma_p
$$

따라서 위험-수익률 조합은 다음과 같은 직선으로 나타나며, 이것이 바로 **자본시장선(CML)** 입니다.

$$
R_C = R_f + \frac{R_p - R_f}{\sigma_p} \sigma_C
$$

---

### 2.3. Lagrange Multiplier를 활용한 최적화 문제

마코위츠 포트폴리오 선택은 다음과 같이 구성됩니다.

$$
\min_{\mathbf{w}} \mathbf{w}^\top \Sigma \mathbf{w} \quad \text{subject to} \quad \mathbf{w}^\top \mathbf{1} = 1, \quad \mathbf{w}^\top \mathbf{R} = \mu
$$

여기서

- \(\mathbf{w}\): 자산별 투자 가중치 벡터 (Risky assets만 고려)
- \(\Sigma\): 공분산 행렬
- \(\mathbf{R}\): 자산의 기대수익률 벡터
- \(\mu\): 목표 기대 수익률

Lagrange 함수는 다음과 같이 정의됩니다.

$$
L(\mathbf{w}, \lambda, \gamma) = \mathbf{w}^\top \Sigma \mathbf{w} - \lambda (\mathbf{w}^\top \mathbf{1} - 1) - \gamma (\mathbf{w}^\top \mathbf{R} - \mu)
$$

편미분을 통해 해를 구하면,

$$
2 \Sigma \mathbf{w} - \lambda \mathbf{1} - \gamma \mathbf{R} = 0
$$

여기서 무위험 자산 \( R_f \)를 도입하면, 투자자는 위험자산포트폴리오와 무위험자산을 조합하는 결정으로 문제 접근을 단순화합니다. 이 과정에서 투자자마다 투자 위험 또는 목표 기대수익률에 따라 최적 비율 \( x \)를 결정하는 형태로 귀결됩니다.

---

### 2.4. Sharpe Ratio와의 관계

자본시장선의 기울기 \( \frac{R_p - R_f}{\sigma_p} \)은 자산 운용에서 **샤프지수(Sharpe Ratio)** 라고 불리며, 가장 높은 샤프지수를 갖는 위험자산 포트폴리오를 시장 포트폴리오라고 명명합니다.

---

## Chapter 3. Python Implementation (구현)

```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from scipy.optimize import minimize

def sharpe_ratio(weights: np.ndarray, returns: np.ndarray, cov_matrix: np.ndarray, risk_free_rate: float) -> float:
    """
    포트폴리오의 샤프지수를 계산합니다.
    Args:
        weights: np.ndarray, 자산별 투자 비율 (합=1)
        returns: np.ndarray, 자산별 기대 수익률 벡터
        cov_matrix: np.ndarray, 자산 공분산 행렬
        risk_free_rate: float, 무위험 수익률
    Returns:
        float: 샤프지수 값 (클수록 좋음)
    """
    port_return = np.dot(weights, returns)
    port_std = np.sqrt(np.dot(weights.T, np.dot(cov_matrix, weights)))
    return (port_return - risk_free_rate) / port_std

def optimize_market_portfolio(returns: np.ndarray, cov_matrix: np.ndarray, risk_free_rate: float) -> np.ndarray:
    """
    샤프지수를 최대화하는 시장 포트폴리오를 구합니다.
    Args:
        returns: np.ndarray, 자산 기대 수익률 벡터
        cov_matrix: np.ndarray, 자산 공분산 행렬
        risk_free_rate: float, 무위험 수익률
    Returns:
        np.ndarray: 최적 가중치 배열
    """
    num_assets = len(returns)
    constraints = {'type': 'eq', 'fun': lambda x: np.sum(x) - 1}  # 투자 비율 합=1
    bounds = tuple((0, 1) for _ in range(num_assets))  # 공매도 금지 조건
    initial_guess = np.full(num_assets, 1/num_assets)  # 균등 분배로 초기화

    def neg_sharpe(weights):
        # 샤프지수 음수(minimize용)
        return -sharpe_ratio(weights, returns, cov_matrix, risk_free_rate)

    opt_result = minimize(neg_sharpe, initial_guess, method='SLSQP', bounds=bounds, constraints=constraints)
    if not opt_result.success:
        raise RuntimeError("Optimization failed:", opt_result.message)
    return opt_result.x

def plot_cml(risk_free_rate: float, market_return: float, market_risk: float) -> None:
    """
    자본시장선(CML)을 플로팅합니다.
    Args:
        risk_free_rate: float, 무위험 수익률
        market_return: float, 시장 포트폴리오 기대수익률
        market_risk: float, 시장 포트폴리오 리스크(표준편차)
    """
    sigma = np.linspace(0, market_risk * 1.5, 100)
    cml = risk_free_rate + (market_return - risk_free_rate) / market_risk * sigma

    plt.figure(figsize=(10, 6))
    plt.plot(sigma, cml, label='Capital Market Line (CML)', color='blue')
    plt.scatter(market_risk, market_return, color='red', marker='*', s=200, label='Market Portfolio')
    plt.scatter(0, risk_free_rate, color='green', marker='o', s=100, label='Risk-Free Asset')
    plt.title("Capital Market Line (CML) with Risk-Free Asset")
    plt.xlabel("Risk (Standard Deviation)")
    plt.ylabel("Expected Return")
    plt.legend()
    plt.grid(True)
    plt.show()

if __name__ == "__main__":
    # 예제 데이터 (연 기대수익률, 연 변동성 기반)
    assets = ['Asset_1', 'Asset_2', 'Asset_3']
    expected_returns = np.array([0.12, 0.10, 0.15])
    cov_matrix = np.array([
        [0.0100, 0.0018, 0.0011],
        [0.0018, 0.0109, 0.0026],
        [0.0011, 0.0026, 0.0199]
    ])
    rf = 0.03  # 무위험 수익률 3%

    # 시장 포트폴리오 최적화 (샤프지수 최대화)
    market_weights = optimize_market_portfolio(expected_returns, cov_matrix, rf)
    market_return = np.dot(market_weights, expected_returns)
    market_risk = np.sqrt(np.dot(market_weights.T, np.dot(cov_matrix, market_weights)))

    print("시장 포트폴리오 가중치:", dict(zip(assets, market_weights)))
    print(f"시장 포트폴리오 기대수익률: {market_return:.4f}")
    print(f"시장 포트폴리오 위험(표준편차): {market_risk:.4f}")

    plot_cml(rf, market_return, market_risk)
```

---

### 코드 설명

- 사용자 정의 함수 `sharpe_ratio`는 주어진 포트폴리오 비중에 따른 샤프지수를 계산합니다.
- `optimize_market_portfolio` 함수는 SciPy의 `minimize`를 통해 샤프지수를 최대화하는 시장포트폴리오를 찾습니다. 여기서는 공매도 제한과 투자비율 합 제약 조건을 둡니다.
- `plot_cml` 함수는 무위험 자산과 시장 포트폴리오 조합으로 이루어진 CML을 시각적으로 표현합니다.
- `__main__` 영역에서는 임의의 예제 자산 3개에 대해 기대수익률과 공분산 행렬을 정의해 실제 최적화 및 플롯을 수행합니다.

---

## Chapter 4. Real World & Critical View (한계와 비평)

---

### 4.1. 이론의 아름다움과 현실의 괴리

자본시장선(CML) 이론은 금융공학史상 혁신적인 도약을 의미했으나, 다음과 같은 현실적 한계가 존재합니다.

- **무위험 자산의 가정은 현실과 다르다**  
  - 무위험 자산의 개념 자체가 완벽한 무위험을 보장하지 않고, 인플레이션과 신용위험, 즉시 유동성 확보의 문제 등이 존재합니다.
  
- **투자자 모두 동일한 투자 기간과 위험선호 가정**  
  - 현실 투자자는 기간과 위험선호가 다양하여 모두가 단일 시장 포트폴리오에 투자한다는 가정이 비현실적입니다.

- **시장 완전성과 정보의 비대칭 문제**  
  - 자본시장은 완전경쟁 시장과 같은 이상적인 조건에 근거하지만, 실제 시장에선 거래비용, 세금, 제한된 정보, 심리적 요인 등 비이상적 요소 다수 존재.

- **공분산 및 기대수익률 추정의 불확실성**  
  - 공분산 행렬과 기대수익률을 정확히 추정하는 것이 어렵고, 과거 데이터에 기반한 추정은 미래를 보장하지 못합니다.

---

### 4.2. 실전 사례 비판

- **Long-Term Capital Management (LTCM) 사태 (1998년)**  
  - LTCM은 CML과 CAPM, 블랙-숄즈 모형을 기반으로 위험관리를 수행했으나 예상치 못한 시장 변동성과 레버리지 과다가 붕괴의 원인이 됨.
- **2008년 금융위기**  
  - 이론이 무시한 신용위험과 유동성위험 증폭, 금융시장 붕괴는 CML과 같은 단순 균형모형의 한계를 드러냄.

---

### 4.3. 실무에서의 보완 및 조정법

- **무위험 자산 대신 리스크 조정 수익률(ERC, CVaR) 활용**  
  - 최근에는 VaR 또는 CVaR와 같은 위험 측정기법을 통해 투자 위험을 조절.
- **블랙-리터만 모델(Black-Litterman)**  
  - 기대수익률을 투자자의 의견과 시장 균형을 결합하여 더 현실적인 포트폴리오 기대수익률 추정.
- **동적 포트폴리오 재조정 및 시뮬레이션 기법 활용**  
  - Monte Carlo 시뮬레이션을 통한 스트레스 테스트 및 동적 자산배분 전략 시행.
- **제한조건을 반영한 최적화 (공매도 제한, 거래비용 포함 등)**  
  - 현실적인 시장 상황 반영을 위한 다중 제약조건 추가.

---

## 마치며

자본시장선(CML)은 금융공학과 투자론에서 무위험 자산 도입에 따라 전통적 MPT를 확장시킨 중추적 개념입니다. 이론적 엄밀함과 직관적 투자 전략을 연결하는 다리 역할을 함과 동시에, 투자 의사결정 체계의 표준을 제시했습니다. 하지만 이론적 완성도가 높음에도 불구하고 실무와 현실 금융시장은 복잡성과 불확실성을 내포하므로, CML과 MPT는 반드시 현실 조건에 맞는 보완과 검증을 거쳐야만 유용한 도구가 됨을 명심해야 합니다.

여러분께서는 이 강의를 바탕으로, 금융수학의 기본 원리를 컴퓨터 공학적 사고와 결합하여 심도 깊은 투자전략 구현 역량을 함양하길 기대합니다.

---

### 참고 문헌

- Markowitz, H. (1952). Portfolio Selection. The Journal of Finance, 7(1), 77-91.
- Sharpe, W. F. (1964). Capital Asset Prices: A Theory of Market Equilibrium under Conditions of Risk. The Journal of Finance, 19(3), 425-442.
- Fabozzi, F. J., Gupta, F., & Markowitz, H. M. (2002). The Legacy of Modern Portfolio Theory. The Journal of Investing, 11(3), 7-22.

---

[끝]