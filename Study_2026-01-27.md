# Chapter 1. The Theoretical Origins (이론적 기원)

2026년 1월 27일부터 시작하는 이 12주 과정의 첫 강의 주제는 “불확실성과 효용 (Utility Theory)”이며, 특히 베르누이의 역설과 기대 효용 이론에 초점을 맞춥니다. 이 장에서는 왜 효용 이론이 고전적 금융이론의 탄생에 필수적이었는지 역사적·논리적 맥락에서 살펴보겠습니다.

금융공학의 역사에서 불확실성은 늘 핵심 문제였습니다. 17~18세기에는 도박과 보험에서 유래한 기대값(Expected Value) 개념이 널리 통용되었으나, 기대값에만 의존하는 의사결정 모델은 모순된 사례들을 해결하지 못했습니다. 대표적 예가 바로 **베르누이의 역설**입니다. 

1700년대 후반, 스위스 수학자 다니엘 베르누이(Daniel Bernoulli)는 단순한 기대값 계산이 실생활 인간의 행동을 설명할 수 없다는 점에 주목했습니다. 예를 들어 다음과 같은 도박 게임을 살펴봅시다.

> 게임 A: 1,000달러를 받는 보장된 수익  
> 게임 B: 50% 확률로 2,500달러, 50% 확률로 0달러

기대값은 게임 A가 1,000달러, 게임 B가 1,250달러로 게임 B가 더 높습니다. 그러나 많은 사람은 안정적으로 1,000달러를 받는 게임 A를 선택합니다. 이 현상은 기대값만으로는 인간의 선택을 설명하지 못함을 보여줍니다. 이것이 바로 베르누이의 역설입니다.

베르누이는 이 문제를 해결하기 위해 ‘효용 함수(utility function)’라는 개념을 도입했습니다. 즉, 단순한 수익 수준보다 그 수익이 주는 만족감(효용)을 기준으로 의사결정해야 한다고 본 것입니다. 효용 함수는 대체로 불확실성에 둔감한 선형 함수가 아니라, 수익이 커질수록 증분 만족감이 줄어드는 오목한 함수 형태를 가집니다(즉, 위험회피 성향을 내포). 이렇게 하면 기대 효용(Expected Utility)이 기대값보다 더 현실적 행태를 모델링할 수 있습니다.

경제학 및 금융에서는 이런 효용 이론이 심리적·도덕적 판단을 계량화하는 도구로 자리 잡았고, 20세기 중반 이후 포트폴리오 이론(Markowitz), 자본자산가격결정모형(CAPM), 옵션가격이론(Black-Scholes) 등 수많은 금융모형의 기반이 되었습니다. 

요약하면, 베르누이의 역설은 금융의 불확실성 하에 의사결정 이론이 발전하는 계기를 마련했고, 기대 효용 이론은 고전 금융 이론의 근간으로 자리 잡았습니다. 이렇게 탄생한 효용 이론은 수학적 엄밀성과 경제적 직관을 결합한 강력한 도구입니다.

---

# Chapter 2. Mathematical Rigor (수학적 증명)

이번 장에서는 기대 효용 이론의 핵심 수식을 엄밀하게 유도해 보겠습니다. 기본 출발점은 불확실한 결과가 도출되는 확률 공간 $(\Omega, \mathcal{F}, P)$와 이에 대한 효용 함수 $U : \mathbb{R} \to \mathbb{R}$입니다. 

### 1. 기대값(Expected Value)의 한계

먼저 기대값의 정의는 다음과 같습니다. 

결과가 확률변수 $X$로 표현될 때, 확률밀도 함수 $f_X(x)$가 존재한다면

$$
\mathbb{E}[X] = \int_{-\infty}^{\infty} x f_X(x) dx
$$

이는 각 결과의 산술평균 가중치이지만, 베르누이의 역설에서 본 바와 같이 경제주체의 심리를 반영하지 못합니다.

### 2. 기대 효용(Expected Utility)

다음으로 베르누이가 제안한 효용 함수 $U(x)$를 도입합니다. 여기서 $x$는 수익 또는 부의 수준입니다. 효용 함수는 일반적으로 다음 조건을 만족합니다.

- 연속적이고 단조증가 함수: $U'(x) > 0$
- 오목 함수 (위험회피성 반영): $U''(x) < 0$

이제 확률변수 $X$의 기대 효용은

$$
\mathbb{E}[U(X)] = \int_{-\infty}^\infty U(x) f_X(x) dx
$$

로 정의됩니다.

### 3. 효용 함수의 예: 로그 효용과 제곱근 효용

대표적인 효용 함수로 다음과 같은 함수들을 사용합니다.

- 로그 효용 함수: 
$$
U(x) = \ln(x), \quad x > 0
$$

- 제곱근 효용 함수:
$$
U(x) = \sqrt{x}, \quad x \geq 0
$$

이들 함수를 통해 위험에 대한 태도가 반영되며, 오목성에 따라 위험을 회피한다는 개념이 명확해집니다.

### 4. 의사결정 규칙: 기대 효용 극대화

경제주체는 선택지들 $\{X_i\}_{i=1}^n$ 에 대해 기대 효용을 계산하고,

$$
\text{max}_{i} \ \mathbb{E}[U(X_i)]
$$

를 만족하는 $X_i$를 선택합니다.

### 5. 수학적 정제: 위험회피도 (Arrow-Pratt measure)

효용 함수의 오목도를 수치화하는 척도는 위험회피도입니다.

$$
r(x) = -\frac{U''(x)}{U'(x)}
$$

- $r(x) > 0$: 위험회피
- $r(x) = 0$: 위험중립
- $r(x) < 0$: 위험선호

이 척도가 의사결정자의 위험성향을 구체적으로 정량화합니다.

### 6. 효용이론과 최적화 문제

효용함수를 최대화하는 의사결정 과정은 최적화 문제로 환원됩니다. 예를 들어, 어떤 확률분포에서 최적의 투자 비중 $\theta$를 찾는 문제로 나타낼 수 있습니다.

$$
\max_{\theta} \mathbb{E}[U(X(\theta))]
$$

여기서 $X(\theta)$는 투자 결과를 나타내는 확률변수입니다. 이 문제는 보통 제약조건 하에서 Lagrange multiplier 기법 등이 동원되어 풀이됩니다.

- 예시: 자본예산 제약, 예산 집행 한계 등

---

# Chapter 3. Python Implementation (구현)

이번 장에서는 기대 효용 이론의 기본 개념과 수식을 Python 프로그래밍으로 구현해 보겠습니다. 대상은 확률분포(예, 이항분포) 아래 두 투자안의 기대 효용을 산출하고 비교하는 간단한 예제입니다. 

```python
import numpy as np
from typing import Callable, List

def expected_utility(outcomes: List[float], probabilities: List[float], utility_func: Callable[[float], float]) -> float:
    """
    주어진 결과(outcomes)와 확률(probabilities)에 대해 기대 효용을 계산합니다.

    Args:
        outcomes (List[float]): 가능한 결과의 리스트.
        probabilities (List[float]): 각 결과가 발생할 확률의 리스트.
        utility_func (Callable[[float], float]): 효용함수.

    Returns:
        float: 기대 효용 값.
    """
    outcomes = np.array(outcomes)
    probabilities = np.array(probabilities)
    utilities = utility_func(outcomes)
    expected_util = np.dot(probabilities, utilities)
    return expected_util

def log_utility(x: float) -> float:
    """로그 효용 함수 (x > 0)"""
    return np.log(x)

def main():
    # 투자안 A: 확실한 1000 달러
    outcomes_A = [1000]
    probs_A = [1.0]

    # 투자안 B: 50% 확률 2500 달러, 50% 확률 0 달러
    outcomes_B = [2500, 0]
    probs_B = [0.5, 0.5]

    eu_A = expected_utility(outcomes_A, probs_A, log_utility)
    eu_B = expected_utility(outcomes_B, probs_B, log_utility)

    print(f"투자안 A의 기대 효용: {eu_A:.4f}")
    print(f"투자안 B의 기대 효용: {eu_B:.4f}")

    if eu_A > eu_B:
        print("투자안 A를 선택합니다. (안정적 선택)")
    else:
        print("투자안 B를 선택합니다. (위험 선호)")

if __name__ == "__main__":
    main()
```

### 주요 내용 설명

1. **expected_utility 함수**: 결과와 확률, 그리고 효용 함수(Callable)를 입력받아 기대 효용 값을 계산합니다. 이 함수는 다양한 효용 함수에 유연하게 적용 가능하며, numpy를 활용하여 벡터화 연산으로 효율을 높였습니다.

2. **log_utility 함수**: 대표적인 위험회피형 효용 함수인 로그 효용을 정의했습니다. 입력값이 0 이하일 경우 에러가 발생하므로 실제 데이터 사용 시 예외 처리가 필요합니다.

3. **main 함수**: 베르누이의 역설에서 다룬 두 투자안의 기대 효용을 계산하여 비교합니다. 

실행 결과는 다음과 같습니다.

```
투자안 A의 기대 효용: 6.9078
투자안 B의 기대 효용: -inf
투자안 A를 선택합니다. (안정적 선택)
```

(0에 로그를 취하기 때문에 `-inf`로 나오는 문제는 실제 구현 시 `x > 0` 조건을 강화해야 합니다.)

---

# Chapter 4. Real World & Critical View (한계와 비평)

여기까지 기대 효용 이론은 금융 의사결정 문제에 엄청난 혁신을 일으켰지만, 현실 시장에서는 여러 한계와 비판에 직면해 있습니다.

### 1. 현실 세계에서의 효용 함수 추정 문제

효용 함수는 인간의 주관적 심리에 근거하기 때문에 실제 효용 함수를 정확히 추정하는 것은 매우 어렵습니다. 사람마다 위험 성향이 다르고, 동일인이라도 상황에 따라 변화합니다. 이 때문에 기대 효용 이론의 적용성은 제한적입니다.

### 2. 한계효용 감소와 높은 리스크 간 불일치

실제 금융위기나 극단적 상황에서 투자자 행동은 위험회피(오목)와는 다른 양상을 보이기도 합니다. 예를 들어 '도박사의 오류' 또는 과잉확신, 군중심리 등에 의해 효용 이론이 설명하지 못하는 비합리적 선택이 나타납니다.

### 3. 베르누이의 역설을 일반화한 다른 이론 등장

- **프로스펙트 이론(Prospect Theory)**: Kahneman과 Tversky가 제안한 이론으로, 기대 효용 이론의 심리적 한계를 극복하고자 했습니다. 손실 회피(loss aversion) 등 실험적 결과를 설명하는 데 강력합니다.

### 4. 실무에서의 활용과 한계

- 포트폴리오 최적화에 기대 효용을 직접 활용하기보다는 기대 수익과 분산(위험) 간의 트레이드오프로 정형화하여, 효용 최대화를 평균-분산 최적화 문제로 변형하는 경우가 많습니다.
- 금융기관에서는 리스크 관리 차원에서 효용 개념보다는 VaR(Value at Risk), CVaR(Conditional VaR) 같은 리스크 척도를 선호합니다.
- 현업에서는 효용 기반 모형에 경험적 조정(칼리브레이션), 심리적 요소 반영 등 실무적 수정을 하고 있습니다.

### 5. Practice Tip

- 기대 효용 이론을 깊게 이해하되, 실제 금융모델링 시에는 현장 상황과 투자자의 행동적 특성을 함께 고려하는 것이 필수입니다.
- Python 구현 시, 효용 함수의 적절한 정의와 경계 조건 처리가 중요하고, 실제 투자 데이터와 시뮬레이션을 함께 활용해 모형 타당성을 검증해야 합니다.

---

# 마무리

이번 강의에서 다룬 베르누이의 역설과 기대 효용 이론은 금융공학이론의 기초를 이루며, 금융상품의 가격 결정과 위험관리에 필수적인 원리를 제공합니다. 수학과 심리를 융합한 이론적 틀 덕분에 금융산업은 불확실성하에서도 합리적 의사결정을 모색할 수 있었습니다. 하지만 이론만으로는 완벽하지 않으므로 향후 강의에서 현대적 비판이론과 모델 확장을 다루며 깊이를 더해 나가겠습니다.