---
# 마코위츠와 포트폴리오 이론 (Modern Portfolio Theory, MPT)  
## Day 2: 효율적 투자선 (Efficient Frontier) 유도

---

## Chapter 1. The Theoretical Origins (이론적 기원)

1950년대 금융공학과 자산관리 분야는 변혁의 시기를 맞이했습니다. 당시 투자자들은 개별 자산의 수익률에만 주목했을 뿐, 자산 간의 상관관계와 그로 인한 리스크 감소 효과에 대해 체계적으로 접근하지 못했습니다. 해리 마코위츠(Harry Markowitz)는 이러한 단편적 접근의 한계점을 인식하고, 투자포트폴리오 내에서 자산들의 결합효과를 최적화하여 리스크를 줄이는 정량적 방법론을 제안했습니다.

특히 마코위츠는 포트폴리오 분산의 개념을 도입, 여러 자산의 수익률 간 공분산(Covariance)의 존재를 밝혀내면서 '분산을 최소화하는 포트폴리오'의 개념을 체계화했습니다. 이는 투자자가 단순히 기대수익률뿐 아니라 리스크 척도인 분산을 함께 고려해야 함을 의미하며, 금융 시장에서 효율적 투자의 새로운 패러다임을 열었습니다.

마코위츠 이전까지 경제학 및 투자 이론에서는 개별 자산의 평균 수익률과 위험을 개별적으로만 평가했으며, 자산 간 상호의존성이나 다각화의 효과를 수학적으로 모형화하는 시도는 매우 제한적이었습니다. 마코위츠의 기법은 공분산 행렬을 사용해 위험을 정량적으로 측정하고, 투자 결정에 있어 수학적 최적화 문제로 접근할 근거를 제공했습니다.

이러한 배경 속에서 '효율적 투자선'이라는 개념이 등장합니다. 이는 주어진 리스크 수준에서 기대 수익률을 최대화하거나, 기대 수익률을 고정했을 때 분산을 최소화하는 포트폴리오들의 궤적입니다. 오늘날 금융시장에서는 이 이론을 바탕으로 다양한 위험관리 전략과 자산배분 기법이 생겨났으며, 전 세계 금융기관들은 마코위츠의 이론을 퀀트 투자 알고리즘의 기본 틀로 사용하고 있습니다.

한편, 이 이론은 노벨경제학상 수상이라는 영광뿐 아니라, 뒤이어 발전된 자본자산가격결정모형(CAPM)과 옵션가격모형(Black-Scholes) 등 현대 금융공학의 기반이 되었습니다. 따라서 MPT는 금융이론사에서 단순 투기 단계를 넘어 체계적인 투자과학으로 도약하는 데 필수적인 이정표 역할을 했음을 이해하는 것이 매우 중요합니다.

---

## Chapter 2. Mathematical Rigor (수학적 증명)

### 2.1 문제정의 및 목표 함수

포트폴리오 내 $n$개의 자산이 있다고 가정합시다. 각 자산의 기대수익률은 벡터

$$
\mathbf{\mu} = 
\begin{bmatrix}
\mu_1 \\
\mu_2 \\
\vdots \\
\mu_n
\end{bmatrix}
$$

이고, 자산 수익률 간 공분산 행렬은

$$
\Sigma = 
\begin{bmatrix}
\sigma_{11} & \sigma_{12} & \cdots & \sigma_{1n} \\
\sigma_{21} & \sigma_{22} & \cdots & \sigma_{2n} \\
\vdots & \vdots & \ddots & \vdots \\
\sigma_{n1} & \sigma_{n2} & \cdots & \sigma_{nn}
\end{bmatrix}
$$

입니다. $ \sigma_{ij} = \mathrm{Cov}(R_i, R_j) $를 의미합니다.

포트폴리오의 자산 배분 비율(가중치)은

$$
\mathbf{w} =
\begin{bmatrix}
w_1 \\
w_2 \\
\vdots \\
w_n
\end{bmatrix}
$$

로 표현하며, $w_i$는 자산 $i$에 투자하는 비율입니다.

이때 포트폴리오의 기대 수익률과 분산은 각각

$$
R_p = \mathbf{w}^T \mathbf{\mu}
$$

및

$$
\sigma_p^2 = \mathbf{w}^T \Sigma \mathbf{w}
$$

로 정의됩니다.

### 2.2 제약조건

1. 전 자산 비중의 합은 1로, 모든 자본이 투자되어야 하므로

$$
\sum_{i=1}^n w_i = \mathbf{1}^T \mathbf{w} = 1
$$

2. 일부 모형에서는 단기 매도 공매도를 금지하여 각 가중치가 음수가 아니도록 제한하기도 합니다:

$$
w_i \ge 0, \quad \forall i=1,2,\ldots,n
$$

오늘은 공매도 허용 상황을 가정합니다.

### 2.3 최적화 문제 설정

고정된 기대수익률 $R_p = r$를 목표로, 이 분산을 최소화하는 포트폴리오 가중치를 찾는 문제는 다음과 같습니다.

$$
\begin{aligned}
& \min_{\mathbf{w}} \quad \mathbf{w}^T \Sigma \mathbf{w} \\
& \text{subject to} \quad \mathbf{w}^T \mathbf{\mu} = r, \\
& \quad \quad \quad \mathbf{1}^T \mathbf{w} = 1.
\end{aligned}
$$

이는 제약조건이 있는 2차 형식의 quadratic programming 문제로, Lagrange 승수법으로 풀 수 있습니다.

### 2.4 Lagrangian 설정

두 개의 제약조건을 포함한 Lagrangian 함수는

$$
\mathcal{L}(\mathbf{w}, \lambda, \gamma) = \mathbf{w}^T \Sigma \mathbf{w} - \lambda (\mathbf{w}^T \mathbf{\mu} - r) - \gamma (\mathbf{1}^T \mathbf{w} - 1)
$$

입니다.

### 2.5 최적해 도출

$\mathbf{w}$에 대해 편미분 후 0으로 놓으면

$$
\frac{\partial \mathcal{L}}{\partial \mathbf{w}} = 2 \Sigma \mathbf{w} - \lambda \mathbf{\mu} - \gamma \mathbf{1} = \mathbf{0}
$$

즉,

$$
2 \Sigma \mathbf{w} = \lambda \mathbf{\mu} + \gamma \mathbf{1}
$$

양변에 $\Sigma^{-1}$를 곱해주면

$$
\mathbf{w} = \frac{1}{2} \Sigma^{-1} (\lambda \mathbf{\mu} + \gamma \mathbf{1})
$$

### 2.6 $\lambda$와 $\gamma$ 결정

위 $\mathbf{w}$는 제약조건을 만족해야 하므로 두 식을 만족해야 합니다.

$$
\begin{cases}
\mathbf{w}^T \mathbf{\mu} = r \\
\mathbf{1}^T \mathbf{w} = 1
\end{cases}
$$

식에 대입하면

$$
\begin{cases}
\frac{1}{2} (\lambda \mathbf{\mu} + \gamma \mathbf{1})^T \Sigma^{-1} \mathbf{\mu} = r \\
\frac{1}{2} (\lambda \mathbf{\mu} + \gamma \mathbf{1})^T \Sigma^{-1} \mathbf{1} = 1
\end{cases}
$$

이를 행렬식으로 풀기 위해 다음과 같은 변수들을 정의합니다.

$$
A = \mathbf{1}^T \Sigma^{-1} \mathbf{1}, \quad
B = \mathbf{1}^T \Sigma^{-1} \mathbf{\mu}, \quad
C = \mathbf{\mu}^T \Sigma^{-1} \mathbf{\mu}
$$

연립방정식은

$$
\begin{bmatrix}
C & B \\
B & A
\end{bmatrix}
\begin{bmatrix}
\lambda/2 \\
\gamma/2
\end{bmatrix}
=
\begin{bmatrix}
r \\
1
\end{bmatrix}
$$

해당 선형시스템을 풀면

$$
\begin{bmatrix}
\lambda/2 \\
\gamma/2
\end{bmatrix}
=
\frac{1}{AC - B^2}
\begin{bmatrix}
A & -B \\
-B & C
\end{bmatrix}
\begin{bmatrix}
r \\
1
\end{bmatrix}
$$

따라서,

$$
\lambda = 2 \frac{A r - B}{AC - B^2}, \quad \gamma = 2 \frac{C - B r}{AC - B^2}
$$

최종 가중치는

$$
\boxed{
\mathbf{w}^* = \Sigma^{-1} \left[ \frac{A r - B}{AC - B^2} \mathbf{\mu} + \frac{C - B r}{AC - B^2} \mathbf{1} \right]
}
$$

### 2.7 효율적 투자선

이렇게 $r$를 여러 값으로 바꿔 최적가중치 $\mathbf{w}^*(r)$를 구하고, 각 포트폴리오의 리스크-수익 쌍 $(\sigma_p(r), r)$을 연결하면 곡선 형태의 효율적 투자선을 얻습니다.

포트폴리오 분산은 다음과 같이 $r$의 2차 함수입니다.

$$
\sigma_p^2(r) = \frac{A r^2 - 2 B r + C}{AC - B^2}
$$

이는 최소 분산 곡선(Minimum Variance Curve)이며, 위에서 구한 $\mathbf{w}^*$는 이 곡선 위의 점입니다. 효율적 투자선은 이 곡선 중에서 투자자가 선택 가능한 구간, 즉 $r$ 값에 따라 위험과 수익이 최적 조화된 부분을 의미합니다.

---

## Chapter 3. Python Implementation (구현)

아래 코드는 위에서 유도한 효율적 투자선을 Python으로 구현한 예시입니다.  
`numpy`는 행렬 연산을 위해, `pandas`는 데이터 관리용, `scipy.optimize`는 최적화 라이브러리로 사용됩니다.

```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from typing import Tuple


def calculate_efficient_frontier(
    mu: np.ndarray, sigma: np.ndarray, points: int = 100
) -> Tuple[np.ndarray, np.ndarray, np.ndarray]:
    """
    효율적 투자선을 계산하는 함수입니다.

    Parameters
    ----------
    mu : np.ndarray
        자산별 기대 수익률 벡터 (n자산).
    sigma : np.ndarray
        자산간 공분산 행렬 (n x n).
    points : int, optional
        효율적 투자선 곡선을 그릴 포인트 수, 기본값은 100.

    Returns
    -------
    risks : np.ndarray
        효율적 투자선에 따른 포트폴리오 위험(표준편차) 배열.
    returns : np.ndarray
        각 포인트별 기대 수익률 배열.
    weights : np.ndarray
        각 포인트별 최적화된 자산 배분 가중치 배열 (points x n).
    """
    # 기초 변수 정의
    inv_sigma = np.linalg.inv(sigma)
    ones = np.ones_like(mu)

    A = float(ones.T @ inv_sigma @ ones)
    B = float(ones.T @ inv_sigma @ mu)
    C = float(mu.T @ inv_sigma @ mu)
    D = A * C - B**2

    # 목표 기대수익률 범위 설정
    min_return = np.min(mu)
    max_return = np.max(mu)
    target_returns = np.linspace(min_return, max_return, points)

    risks = np.zeros(points)
    weights = np.zeros((points, len(mu)))

    for i, r in enumerate(target_returns):
        # Lagrange 승수 계산
        lam = 2 * (A * r - B) / D
        gamma = 2 * (C - B * r) / D

        # 최적 가중치 계산
        w = inv_sigma @ (lam * mu + gamma * ones) / 2

        weights[i, :] = w
        risks[i] = np.sqrt(w.T @ sigma @ w)

    return risks, target_returns, weights


def plot_efficient_frontier(risks: np.ndarray, returns: np.ndarray) -> None:
    """
    효율적 투자선 그래프를 그리는 함수입니다.

    Parameters
    ----------
    risks : np.ndarray
        포트폴리오 위험(표준편차) 배열.
    returns : np.ndarray
        기대 수익률 배열.

    Returns
    -------
    None
    """
    plt.figure(figsize=(10, 6))
    plt.plot(risks, returns, 'b-', label='Efficient Frontier')
    plt.xlabel('Portfolio Risk (Standard Deviation)')
    plt.ylabel('Portfolio Expected Return')
    plt.title('Markowitz Efficient Frontier')
    plt.legend()
    plt.grid(True)
    plt.show()


if __name__ == "__main__":
    # 예시: 4자산 기대수익률과 공분산 행렬 (연수익률 단위)
    mu = np.array([0.10, 0.12, 0.15, 0.09])
    sigma = np.array([
        [0.005, -0.010, 0.004, -0.002],
        [-0.010, 0.040, -0.002, 0.004],
        [0.004, -0.002, 0.023, 0.002],
        [-0.002, 0.004, 0.002, 0.010]
    ])

    risks, returns, weights = calculate_efficient_frontier(mu, sigma)
    plot_efficient_frontier(risks, returns)
```

### 코드 해설

- `calculate_efficient_frontier` 함수에서는 공분산 행렬의 역행렬을 구해 행렬 연산으로 효율적 투자선 수식의 핵심 파라미터들을 안정적으로 계산합니다.
- 연속된 기대수익률 값들을 대상으로 최적화 문제를 풀이해 자산별 최적 투자 비중을 구합니다.
- 이후 `plot_efficient_frontier` 함수로 리스크-수익률 평면에 효율적 투자선을 시각화합니다.
- 해당 코드를 통해 투자자는 다양한 기대수익률에 따른 포트폴리오 구성비중과 리스크 트레이드오프를 명확히 시각적으로 파악할 수 있습니다.

---

## Chapter 4. Real World & Critical View (한계와 비평)

### 4.1 현실 시장에서의 제약

마코위츠의 효율적 투자선 이론은 학문적으로 빛나는 모델임에도 불구하고, 현실 투자 환경에서는 다음과 같은 한계와 도전에 직면합니다.

#### 1. 입력 변수 추정 문제

- **기대수익률과 공분산 행렬 추정의 불확실성 문제**: 과거 데이터를 기반으로 추정하는 수식 내 기대수익률 벡터 $\mu$와 공분산 행렬 $\Sigma$는 샘플링 오류와 모델 불안정성에 취약합니다.
- 이는 곧 효율적 투자선의 위치를 크게 변화시키며 실제 투자성과와 괴리가 발생하는 원인이 됩니다.
- 특히 기대수익률은 분산에 비해 추정 오차가 매우 크며, 포트폴리오 최적화에 미치는 영향이 급격히 커서 견고하지 못합니다.

#### 2. 시장의 비정상성

- 마코위츠 모델은 자산수익률이 정규분포에 따르며, 통계적 성질이 일정하다고 가정하지만 실제 금융시장은 비정상적이고 시계열적 자기상관, 시장 충격 등 비정상성을 내포합니다.
- 이로 인해 공분산만을 리스크로 정의하는 것이 불충분합니다.

#### 3. 거래비용, 세금 및 공매도 제한

- 현실 세계에서는 빈번한 리밸런싱에 따른 거래비용과 세금이 수익률에 영향을 미치며, 공매도가 불가능한 자산도 존재합니다.
- 이와 같은 제약 조건을 고려한 확장 모델이 필요합니다.

### 4.2 실패 사례와 교훈

- 1998년 LTCM(Long-Term Capital Management) 파산 사례는 MPT와 CAPM을 근간으로 구축된 전략의 한계점을 보여줍니다.
- 해당 헤지펀드는 시장이 정규분포를 따른다는 가정 아래 최적 포트폴리오를 구성했으나, 러시아 금융위기 등 극단적 이벤트 발생 시 대규모 손실이 발생해 결국 청산되었습니다.
- 이는 금융 시장의 극단 리스크를 전통적인 분산-수익률 프레임워크에서 제대로 포착하지 못한 한계로 해석됩니다.

### 4.3 실무에서의 확장 방향과 팁

- **견고성 강화 (Robust Optimization)**: 불확실성 영역을 포함한 최적화 방법론을 적용해 추정 오류에 강한 포트폴리오를 설계합니다.
- **분산 대신 Value at Risk (VaR), Conditional VaR (CVaR) 사용**: 리스크 지표 다양화로 극단 손실에 대응합니다.
- **정규분포 가정 파괴**: 분포 비대칭, 두꺼운 꼬리(t-분포 등)를 반영하거나 비모수적 접근법 사용.
- **제약조건 추가**: 공매도 금지, 최소·최대 투자비중 제한, 자산군 분리 등 실무적 제약을 최적화 문제에 반영합니다.
- **동적 리밸런싱과 강화학습 도입**: 시장 상황 변화에 맞춘 적응적 투자 전략 구현.

---

# 요약

이번 강의에서는 마코위츠의 효율적 투자선 개념을 역사적 맥락과 수학적 엄밀성을 통해 이해하고, Python 코드를 통한 실습 준비를 마쳤습니다. 또한 이 모델이 갖는 이상적 이론적 미덕과 동시에 현실 시장에서의 한계점, 그리고 실무 응용에서의 주의사항을 폭넓게 논의했습니다.

앞으로 이러한 근본 개념을 바탕으로 실제 금융 데이터에 대한 적절한 적용과 확장 방법을 습득하는 것이 퀀트 투자자로서의 필수 역량임을 명심하시기 바랍니다.

---

# 참고 문헌

- Markowitz, H. (1952). Portfolio Selection. *The Journal of Finance*, 7(1), 77-91.
- Elton, E. J., Gruber, M. J., Brown, S. J., & Goetzmann, W. N. (2009). *Modern Portfolio Theory and Investment Analysis*. Wiley.
- Fabozzi, F. J., Huang, D., & Zhou, G. (2010). Robust portfolios: Contributions, limitations, and linkages to factor models. *European Journal of Operational Research*, 207(2), 543-560.
- Michaud, R. (1998). Efficient Asset Management: A Practical Guide to Stock Portfolio Optimization and Asset Allocation. Harvard Business School Press.

---

# 다음 강의 준비 과제

- 오늘 배운 효율적 투자선 수식과 코드를 바탕으로, 직접 주어진 금융 데이터(예: S&P 500 대표 자산군)로 효율적 투자선을 그려보십시오.
- `scipy.optimize` 활용한 추가적 Quadratic Programming 풀이법을 익히고 비교해보는 것을 권장합니다.
- 효율적 투자선 개념의 확장인 자본시장선(Capital Market Line, CML)과 이를 통한 Sharpe Ratio 최대화 문제도 예습해주시기 바랍니다.

감사합니다.