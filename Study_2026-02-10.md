# CAPM의 실패 ("Beta is Dead") — 대학원 심층 강의 노트

---

## Chapter 1. The Theoretical Origins (이론적 기원)

자산 가격 결정 이론의 핵심 틀로서, CAPM (Capital Asset Pricing Model)은 1960년대에 해리 마코위츠(Harry Markowitz)의 포트폴리오 선택 이론과 윌리엄 샤프(William Sharpe), 존 린트너(John Lintner), 그리고 잭 트레노르(Jack Treynor)에 의해 독립적이고 체계적으로 발전되었습니다. 마코위츠가 포트폴리오 평균 수익률과 리스크(분산)의 무역 관계를 수학적으로 규명함으로써 '최적 분산투자' 개념을 도입했지만, 투자자들이 왜 특정 자산을 선호하는지 "위험 프리미엄"의 정량적 이유는 명확하지 않았습니다.

이 시점에서 CAPM은 “위험”을 시장 전체 위험의 표준, 즉 Beta 계수를 통해 단일 척도로 환원할 수 있다는 혁신적 아이디어를 제시합니다. Beta는 특정 자산과 시장 포트폴리오 간 수익률 공분산이 시장 수익률 분산으로 표준화된 계수로, “비체계적 위험”은 분산투자로 제거 가능하며, 오직 “체계적 위험(시장 위험)”만이 투자자의 위험 프리미엄 보상의 대상이 되어야 한다는 대원칙에 바탕합니다.

CAPM의 등장은 당시 자산 가격 결정 모델들이 시장 데이터에 과도하게 적합화되어 있거나, 선수 투자자들의 행동 심리까지 복잡하게 다루었던 상황과 대조적입니다. 단일 팩터가 자산 수익률을 설명한다는 단순하지만 세련된 이론적 단초는 시장 균형 이론의 수학적 엄밀함과 투자 현실의 합리적 해석이 만나는 지점이었습니다.

그러나 1970~1980년대 이후 고도화된 금융 데이터 분석과 대체 팩터 모형의 등장과 함께 CAPM의 Beta보다 더 다차원적인 설명력이 요구되었고, 특히 경험적 검증에서 시장 초과수익률을 안정적으로 설명하지 못하는 사례가 속출하면서 ‘Beta is Dead’라는 반어적 결론에 도달하게 됩니다. 이는 곧 다요인모형(예, Fama-French 3요인, Carhart 4요인)의 꽃을 피우는 기반이 되었습니다.

---

## Chapter 2. Mathematical Rigor (수학적 증명)

CAPM의 핵심 수식은 다음과 같습니다.

$$
E\left[R_i\right] = R_f + \beta_i (E[R_m] - R_f)
$$

여기에서,

- $R_i$: 자산 $i$의 기대수익률
- $R_f$: 무위험 수익률
- $R_m$: 시장 포트폴리오 수익률
- $\beta_i$: 자산 $i$의 시장 민감도 (Beta)로 정의됨

$$
\beta_i = \frac{\mathrm{Cov}(R_i, R_m)}{\mathrm{Var}(R_m)}
$$

### CAPM의 유도 과정

1. **포트폴리오 최적화 문제**

투자자는 위험(분산) 대비 기대수익률을 극대화하려고 하며, 이는 다음의 최적화 문제로 표현됩니다.

$$
\max_{\mathbf{w}} \quad \mathbf{w}^\top \mathbf{\mu} - \frac{\lambda}{2} \mathbf{w}^\top \mathbf{\Sigma} \mathbf{w}
$$

여기서,

- $\mathbf{w}$: 자산 가중치 벡터
- $\mathbf{\mu}$: 자산들의 기대수익률 벡터
- $\mathbf{\Sigma}$: 자산 수익률 공분산 행렬
- $\lambda$: 투자자의 위험회피 계수

라그랑주 승수법을 사용하여 예산 제약 조건 $\mathbf{w}^\top \mathbf{1} = 1$을 달성하며 문제를 풉니다.

2. **시장 포트폴리오와 가격 결정**

균형 시장에서는 모든 투자자가 시장 포트폴리오를 최적화한 상태이고, 이 때 자산의 초과수익률과 시장 초과수익률은 선형관계를 가집니다.

3. **베타의 정의와 해석**

Beta는 자산 수익률의 시장 수익률에 대한 회귀계수로서, ‘체계적 위험의 척도’ 역할을 하며,

$$
R_i - R_f = \alpha_i + \beta_i (R_m - R_f) + \epsilon_i
$$

에서 오차항 $\epsilon_i$가 평균 0, 투자자에게 보상되지 않는다는 가정 하에 $\alpha_i=0$이 균형 상태를 의미합니다.

4. **라그랑주 승수 접근**

보다 엄밀히 수학적으로,

라그랑주 함수 식은

$$
\mathcal{L} = \mathbf{w}^\top \mathbf{\mu} - \frac{\lambda}{2} \mathbf{w}^\top \mathbf{\Sigma} \mathbf{w} - \gamma (\mathbf{w}^\top \mathbf{1} - 1)
$$

여기서 편미분 후 최적 조건을 구해,

$$
\mathbf{\mu} - \lambda \mathbf{\Sigma} \mathbf{w} - \gamma \mathbf{1} = 0
$$

해를 행렬 대수적으로 구하고 이를 시장균형 조건과 연관지으면 Beta 수식이 도출됩니다.

---

## Chapter 3. Python Implementation (구현)

이제 CAPM의 핵심 개념인 Beta 계산을 실무에서 어떻게 구현하는지 살펴보겠습니다. 학습 데이터로 주식의 일별 수익률과 시장 수익률 (예: KOSPI, S&P500 등)을 사용하여 Rolling Beta를 구할 수도 있으며, 여기서는 기본 회귀분석을 통한 Beta 추정 코드를 완성합니다.

```python
import numpy as np
import pandas as pd
import statsmodels.api as sm
import matplotlib.pyplot as plt
from typing import Tuple

def calculate_beta(stock_returns: pd.Series, market_returns: pd.Series) -> Tuple[float, float]:
    """
    주어진 주식과 시장 수익률 데이터를 바탕으로 CAPM Beta와 alpha를 회귀분석으로 추정합니다.

    Args:
        stock_returns (pd.Series): 개별 주식의 수익률 (동일 기간)
        market_returns (pd.Series): 시장 포트폴리오 수익률 (동일 기간)

    Returns:
        Tuple[float, float]: alpha (절편), beta (시장 민감도)
    """
    # 독립변수 앞에 상수항 추가
    X = sm.add_constant(market_returns)
    model = sm.OLS(stock_returns, X).fit()
    alpha, beta = model.params['const'], model.params[market_returns.name]
    return alpha, beta

def plot_cumulative_returns(stock_returns: pd.Series, market_returns: pd.Series) -> None:
    """
    누적수익률을 시각화하여 두 시계열 데이터의 퍼포먼스 차이를 보여줍니다.

    Args:
        stock_returns (pd.Series): 주식 수익률 시리즈
        market_returns (pd.Series): 시장 수익률 시리즈
    """
    stock_cum = (1 + stock_returns).cumprod() - 1
    market_cum = (1 + market_returns).cumprod() - 1

    plt.figure(figsize=(10,6))
    plt.plot(stock_cum, label='Stock Cumulative Return')
    plt.plot(market_cum, label='Market Cumulative Return')
    plt.legend()
    plt.title('누적 수익률 비교')
    plt.xlabel('날짜')
    plt.ylabel('수익률')
    plt.grid(True)
    plt.show()

if __name__ == "__main__":
    # 예제: Yahoo Finance에서 받은 수익률 데이터 로드 (CSV 등)
    stock = pd.read_csv('stock_returns.csv', index_col=0, parse_dates=True)
    market = pd.read_csv('market_returns.csv', index_col=0, parse_dates=True)

    # 공통 기간으로 필터링
    common_idx = stock.index.intersection(market.index)
    stock_returns = stock.loc[common_idx, 'return']
    market_returns = market.loc[common_idx, 'return']

    alpha, beta = calculate_beta(stock_returns, market_returns)
    print(f"Estimated CAPM Alpha: {alpha:.5f}, Beta: {beta:.5f}")

    plot_cumulative_returns(stock_returns, market_returns)
```

- `statsmodels` 라이브러리를 활용하여 단순선형회귀모델로 Beta와 Alpha를 추정합니다.
- `pandas`의 시계열 데이터 인덱스를 활용하여 정확한 기간 일치 및 데이터 정제를 수행합니다.
- 누적수익률 시각화는 개별 자산과 시장 간 퍼포먼스 비교의 직관적 이해를 돕습니다.

추가로, Day 3에서 시행한 Rolling Beta 계산 코드를 복습하여 시점별 Beta 변동성도 분석해보세요. 실무에서는 Beta가 시계열 상 일정하지 않고 변화한다는 점이 중요합니다.

---

## Chapter 4. Real World & Critical View (한계와 비평)

CAPM은 금융공학과 자산관리 실무에 혁신을 가져왔지만, 다음과 같은 현실적 한계성을 갖고 있습니다.

### 1. 경험적 검증에서의 실패

- 여러 시계열 및 횡단면 연구 결과, Beta가 실제 주식 초과수익률을 안정적으로 설명하지 못함
- 높은 Beta 주식이 반드시 높은 기대수익을 보장하지 않음 — “역전 현상”이나 “저평가 가치주 고수익”과 같은 이상 현상 존재 (Fama-French 발견)
- 시장 포트폴리오 정의의 모호성: CAPM은 모든 위험자산을 포함하는 ‘시장 포트폴리오’를 가정하지만, 현실에서는 완전한 시장 포트폴리오 가정이 불가능

### 2. 금융 위기 및 시스템 리스크 대응 불가

- LTCM(1998년)과 2008년 글로벌 금융위기에서, CAPM 기반 리스크 측정과 헤징이 한계를 보임
- 체계적 위험이 단일 Beta 하나로 설명 불가능하며, 극단적 이벤트, 유동성 부족, 신용 위험 등 비체계적 요소가 동시다발적으로 작용

### 3. 현실적 투자자 행동과 시장 비효율성 무시

- 투자자 행동경제학 관점의 비합리성, 정보비대칭, 제한된 차익거래 환경 등 CAPM 가정 하에서는 설명할 수 없는 현상 다수

### 실무 팁 (Practice Tip)

- 다요인모델(Fama-French 3/5요인, Carhart 4요인 등)을 도입하여 Beta 외 신뢰성 높은 리스크 인자를 추가 분석
- 최근 머신러닝 기법을 도입해 비선형 관계 및 다양한 이질적 리스크 탐색
- Rolling Beta를 사용해 Beta 변동성을 감안하고, 단기 시점에 과도한 Beta 의존 회피
- 스트레스 테스트와 시나리오 분석을 함께 수행하여 표준 CAPM의 한계에 대비

---

# 마무리

CAPM은 금융공학의 역사에서 ‘위험과 수익의 균형’이라는 자산 가격 결정의 고전적 기준틀을 제공함으로써 학문과 실무에 지대한 영향을 끼쳤습니다. 그러나 현실 시장에서의 다양한 한계와 경험적 실패는 Beta 하나만으로 시장을 설명하는 데 무리가 있음을 경고합니다. 본 강의를 통해 CAPM의 구조적 아름다움과 한계를 동시에 이해하고, 후속 다요인 모형 및 현대 금융공학 기법의 필요성을 체득함으로써 더 심층적인 자산 가격 결정 모형 개발로 나아갈 수 있기를 바랍니다.

--- 

End of Lecture Note.